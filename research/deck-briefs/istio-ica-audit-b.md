# istio-ica audit, part b (topics 5–8)

Cards checked: 239 (05-resilience, 06-authentication, 07-authorization-edge-tls, 08-troubleshooting). Sources: research/sources/istio-ica/. All eight findings are patched in istio-ica-fixes-b.jsonl; none needs research. The patch file passes `node build/apply-fixes.mjs istio-ica research/deck-briefs/istio-ica-fixes-b.jsonl --dry`.

id | severity | problem | evidence (source words, with page)
---|---|---|---
istio.troubleshooting.yaml-extra-dash | wrong | Back says the policy "matches the path or the namespace, not both", which reads as an exclusive or. A request that meets both conditions is still allowed. | "the policy actually allows requests if the path is /foo or the source namespace is foo, which is more permissive." (security-issues, #make-sure-there-are-no-typos-in-the-policy-yaml-file)
istio.authorization-edge-tls.what-is-sni | unsupported | Back says SNI names "the host the client wants". The cited page does not say what SNI is. It says only that the SNI value is matched against hosts. | "For HTTP traffic the HTTP Host/Authority header will be matched against the hosts field. For HTTPs or TLS traffic containing Server Name Indication (SNI), the SNI value will be matched against the hosts field." (service-entry reference, #ServiceEntry)
istio.authentication.what-is-a-jwt | unclear | Back ("The end-user token that request authentication policies hold the values to validate") has to be read twice. | "Used for end-user authentication to verify the credential attached to the request … JSON Web Token (JWT) validation"; "Request authentication policies specify the values needed to validate a JSON Web Token (JWT)." (concepts/security, #authentication, #request-authentication)
istio.authorization-edge-tls.rbac-access-denied | unclear | Front says "allow-nothing policy" but the explanation calls it the "deny-all policy". Earlier cards define deny-all as a different policy (a DENY with an empty rule), so the explanation reads as a contradiction. | "create a allow-nothing policy … You should see "RBAC: access denied". The error shows that the configured deny-all policy is working as intended, and Istio doesn't have any rules that allow any access" (authz-http, #configure-access-control-for-workloads-using-http-traffic)
istio.authorization-edge-tls.single-host-setup | ambiguous | "Istio Gateway APIs" can be read as the Kubernetes Gateway API. The task keeps "Istio APIs" and "Gateway API" in separate tabs, and mode SIMPLE with credentialName belongs to the Istio APIs tab. | "Follow instructions under either the Gateway API or Istio APIs tab"; "The TLS mode should have the value of SIMPLE." (secure-ingress, #configure-a-tls-ingress-gateway-for-a-single-host)
istio.resilience.faults-application-layer | unclear | The front puts the contrast after the question ("At which layer does Istio inject faults, unlike delaying packets or killing pods?"), so it reads oddly and hides "network layer". | "Unlike other mechanisms for introducing errors, such as delaying packets or killing pods at the network layer, Istio lets you inject faults at the application layer." (concepts/traffic-management, #fault-injection)
istio.authorization-edge-tls.action-allow | minor | "What is special about it?" is open to many answers. The back gives only "it is the default action". | "ALLOW Allow a request only if it matches the rules. This is the default type." (authorization-policy reference, #AuthorizationPolicy-Action)
istio.authentication.scenario-token-cases | minor | choicesExplained says an invalid bearer token "is always rejected". The source does not say "always". | "Istio validates the token using the public key set, and rejects requests if the bearer token is invalid." (authn-policy, #end-user-authentication)
