{"id": "istio.troubleshooting.yaml-extra-dash", "field": "back", "value": "The rules are ORed, so the policy allows requests on the path or from the namespace, instead of needing both: more permissive.", "severity": "wrong", "reason": "'not both' reads as exclusive or; the source says the policy allows requests if the path is /foo or the namespace is foo."}
{"id": "istio.authorization-edge-tls.what-is-sni", "field": "back", "value": "A value in HTTPS or TLS traffic that Istio matches against the hosts field, as it matches the Host/Authority header for HTTP.", "severity": "unsupported", "reason": "The cited ServiceEntry reference never says SNI names the host the client wants; it only says the SNI value is matched against hosts, as the Host/Authority header is for HTTP."}
{"id": "istio.authentication.what-is-a-jwt", "field": "back", "value": "A token carrying an end user's credential, which Istio validates using the values set in a request authentication policy.", "severity": "unclear", "reason": "The old back ('the end-user token that request authentication policies hold the values to validate') has to be read twice."}
{"id": "istio.authorization-edge-tls.rbac-access-denied", "field": "explanation", "value": "The task reads this error as proof that its allow-nothing policy works: Istio has no rules that allow any access to the workloads.", "severity": "unclear", "reason": "The old explanation called the policy 'deny-all', which earlier cards define as a different policy (a DENY with an empty rule)."}
{"id": "istio.authorization-edge-tls.single-host-setup", "field": "front", "value": "To expose one HTTPS host through an Istio Gateway resource (the Istio APIs, not the Kubernetes Gateway API), what TLS settings go on the port 443 server?", "severity": "ambiguous", "reason": "'Istio Gateway APIs' can be read as the Kubernetes Gateway API; the task's Istio APIs tab is the one with mode SIMPLE and credentialName."}
{"id": "istio.resilience.faults-application-layer", "field": "front", "value": "Unlike delaying packets or killing pods at the network layer, at which layer does Istio inject faults?", "severity": "unclear", "reason": "The old front put the contrast after the question, so it read as if Istio's layer were 'unlike delaying packets'."}
{"id": "istio.authorization-edge-tls.action-allow", "field": "front", "value": "What does the `ALLOW` action do, and which action does a policy use when none is set?", "severity": "minor", "reason": "'What is special about it?' is open to many answers; the back answers only 'it is the default'."}
{"id": "istio.authentication.scenario-token-cases", "field": "choicesExplained", "value": "A: a request with no token is accepted when only request authentication is configured. B: a request with an invalid bearer token is rejected.", "severity": "minor", "reason": "'always rejected' is stronger than the source, which says Istio rejects requests if the bearer token is invalid."}
