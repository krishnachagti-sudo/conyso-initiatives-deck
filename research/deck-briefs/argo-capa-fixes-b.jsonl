{"id": "argo.rollouts.rollback-window-scenario", "field": "front", "value": "A Rollout has `rollbackWindow` with `revisions: 3` and history 1 to 6, with 6 current. A rollback to which revision is fast-tracked?", "severity": "wrong", "reason": "The window counts the current revision (docs: revisions 3 with 5 current covers 4 and 3), so with revisions 2 and 6 current only 5 is fast-tracked and no choice was right; revisions 3 makes Revision 4 the one right answer."}
{"id": "argo.rollouts.rollback-window-scenario", "field": "choicesExplained", "value": "B, C and D lie outside the window of three revisions (6, 5 and 4), so rolling back to them runs all the steps again.", "severity": "wrong", "reason": "Matches the corrected window size."}
{"id": "argo.rollouts.rollback-window-scenario", "field": "explanation", "value": "The window counts the current revision: in the docs' example, `revisions: 3` with 5 current fast-tracks 4 and 3, so here it covers 5 and 4.", "severity": "wrong", "reason": "The old explanation said a window of 2 covers the two revisions before the current one, which the docs' example contradicts."}
{"id": "argo.rollouts.setweight-scenario", "field": "choicesExplained", "value": "A and D keep all 20 stable pods, but without traffic routing the stable ReplicaSet shrinks as the canary grows. B gives the canary 5%, not 25%.", "severity": "wrong", "reason": "A and D do not describe traffic-routed scaling: with traffic routing the canary would still get 5 pods, not 20 or 25."}
{"id": "argo.rollouts.what-is-workloadref", "field": "front", "value": "In Argo Rollouts, what is `workloadRef`?", "severity": "unclear", "reason": "The old front put a colon and a gloss inside the question, so it read as two questions."}
{"id": "argo.rollouts.canary-no-steps", "field": "explanation", "value": "With no steps there is nothing to pause at or weigh, so the Rollout moves to the new version as a rolling update would, paced by maxSurge and maxUnavailable.", "severity": "unsupported", "reason": "The section does not list this as a use case; the new text explains why without that claim."}
{"id": "argo.cd-patterns.what-is-helm-valuefiles", "field": "back", "value": "The list of values files an Argo CD Helm Application passes to Helm; the declarative form of the repeatable `--values` flag of `argocd app set`.", "severity": "unsupported", "reason": "The docs' --values flag belongs to argocd app set, not to Helm."}
{"id": "argo.cd-patterns.what-is-helm-valuefiles", "field": "explanation", "value": "Helm can take its parameters from a different values file, or several; `valueFiles` records that choice in the Application instead of on a command line.", "severity": "unsupported", "reason": "Drops the claim that the flag is Helm's own."}
{"id": "argo.cd-patterns.what-is-helm-valuefiles", "field": "example", "value": "Like running `argocd app set helm-guestbook --values values-production.yaml`, but written into the Application spec.", "severity": "unsupported", "reason": "Uses the command the docs show instead of helm template."}
{"id": "argo.cd-patterns.tool-detection-chart-yaml", "field": "choicesExplained", "value": "A needs a kustomization file, which the path lacks. C is only the fallback when neither Helm nor Kustomize is detected. D: the detection order in the docs names only Helm, Kustomize and the directory fallback.", "severity": "unsupported", "reason": "The page does not say a plugin is never detected from files, and config management plugins can have discovery rules."}
{"id": "argo.cd-patterns.helm-ls-empty", "field": "explanation", "value": "Helm only renders the manifests; Argo CD applies and tracks them, so there is no Helm release for `helm ls` to list.", "severity": "unsupported", "reason": "The cited text says nothing about helm ls; the new explanation follows from the quoted sentence."}
{"id": "argo.cd-patterns.supported-tools-plugin", "field": "explanation", "value": "The built-in list is Kustomize, Helm charts, OCI images and directories of YAML, JSON or Jsonnet manifests; a plugin covers anything else.", "severity": "minor", "reason": "The old list said plain directories and left out Jsonnet, which the source names."}
{"id": "argo.events.trigger-types-messaging", "field": "front", "value": "Which trigger types publish a message to a messaging system?", "severity": "ambiguous", "reason": "The list has three such types, so asking for two left Azure Event Hubs Messages out."}
{"id": "argo.events.trigger-types-messaging", "field": "back", "value": "Kafka Messages, NATS Messages and Azure Event Hubs Messages.", "severity": "ambiguous", "reason": "Adds the third messaging trigger type from the list."}
{"id": "argo.events.trigger-types-messaging", "field": "evidence", "value": "Kafka Messages - NATS Messages - Slack Notifications - Azure Event Hubs Messages", "severity": "ambiguous", "reason": "Evidence now covers Azure Event Hubs Messages."}
{"id": "argo.events.flow-order-scenario", "field": "choicesExplained", "value": "A starts with the Sensor, which only receives from the bus. D starts with the bus, which only carries what an EventSource has published. C skips the bus between source and Sensor, and puts it after the action.", "severity": "wrong", "reason": "D starts with the EventBus itself, not with a component that receives from the bus."}
{"id": "argo.events.leader-election-opt-in", "field": "back", "value": "By adding the annotation `events.argoproj.io/leader-election: k8s` to the EventSource.", "severity": "unclear", "reason": "A specific annotation told the learner nothing; the source gives it."}
{"id": "argo.events.leader-election-rbac", "field": "back", "value": "A Role allowing `get`, `create` and `update` on `leases` in the `coordination.k8s.io` API group.", "severity": "unclear", "reason": "The RBAC rules the docs list gave no answer to learn; the source gives the rules."}
{"id": "argo.events.conditions-reset", "field": "explanation", "value": "A reset, such as a `byTime` cron at 23:59, clears the events already received, so a lone A cannot wait overnight and pair with the next day's B.", "severity": "unclear", "reason": "Reset all the stakeholders of the conditions is opaque and does not say why it helps."}
{"id": "argo.events.eventbus-deprecated", "field": "explanation", "value": "Of the three implementations, only NATS carries the deprecated label, so JetStream and Kafka are the current choices.", "severity": "unsupported", "reason": "Nothing cached says older material shows NATS Streaming as the default."}
{"id": "argo.events.eventbus-kafka-leader", "field": "explanation", "value": "EventSource high availability uses NATS for leader election by default; a Kafka EventBus switches it to Kubernetes leader election.", "severity": "minor", "reason": "The old explanation was circular."}
{"id": "argo.syncing.scenario-self-heal", "field": "back", "value": "B: nothing. The app shows OutOfSync, but no automated sync runs unless `selfHeal` is true.", "severity": "unclear", "reason": "B: nothing: read as a label followed by another label."}
{"id": "argo.syncing.scenario-synced-no-autosync", "field": "explanation", "value": "Automated sync reacts to sync status, not health: an app that is Synced is left alone even while it is Degraded.", "severity": "unclear", "reason": "Only a Synced or error state stops it had to be read twice."}
