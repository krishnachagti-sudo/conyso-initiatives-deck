# ckad audit B: topics 05–08

Scope: decks/ckad/notes/05-observability-debugging.json to 08-services-ingress-netpol.json, 271 cards (05: 69, 06: 66, 07: 80, 08: 56). Every card was checked against its `evidence` first. Claims beyond the evidence were checked in research/sources/ckad/. No fetches were needed.

Beyond-evidence claims confirmed in the cache: FailedScheduling means "not enough resources for the Pod on any of the nodes" (Debug Running Pods); `--validate` reports `unknown field: commnd` (Debug Pods); `kubectl convert` has install pages for Linux, macOS and Windows (Deprecated API Migration Guide); authentication gets "the entire HTTP request" but typically examines headers or the client certificate (Controlling Access); a memory limit without a request also gets a matching request (Assign CPU Resources, "Similarly…"); `--api-group` filter (kubectl Quick Reference); NotFound after deleting a CRD (CRD task); ConfigMap volume files named after keys, and earlier files made inaccessible (Configure a Pod to Use a ConfigMap); envFrom prefix (Define Environment Variables); empty Secret type treated as Opaque (Secrets); `allowPrivilegeEscalation` defaults to true (Security Context); CoreDNS as the DNS add-on (Connecting Applications with Services); TLS termination with plaintext to Pods (Ingress).

id | severity | problem | evidence (source words, with page)
---|---|---|---
ckad.observability-debugging.app-http-redirect | wrong | The keyed answer says a 302 always counts as success because it is between 200 and 399. The kubelet follows redirects to the same host, so the result then depends on the final response. Only a redirect to a different host is not followed and is counted as success. Patched to test the different-host case, with the probes page as source. | "When the kubelet probes a container using HTTP, it follows redirects only if the redirect is to the same host … If the redirect is to a different hostname, the kubelet does not follow it. Instead, the kubelet treats the probe as successful and records a ProbeWarning event." (Liveness, Readiness, and Startup Probes, "Redirect handling")
ckad.config-secrets-security.fsgroup | wrong | The explanation says fsGroup has no effect on secret, configMap and emptyDir volumes. On the page that note follows the `fsGroupChangePolicy` example and is about that field, not fsGroup. | "fsGroup: 2000 fsGroupChangePolicy: "OnRootMismatch" … This field has no effect on ephemeral volume types such as secret, configMap, and emptyDir." (Configure a Security Context, "Configure volume permission and ownership change policy for Pods")
ckad.config-secrets-security.secret-type-bootstrap | wrong | The explanation says it is "the only built-in type in the table outside the kubernetes.io prefix". Opaque is in the same table with no prefix at all. | "Opaque arbitrary user-defined data kubernetes.io/service-account-token ServiceAccount token … bootstrap.kubernetes.io/token bootstrap token data" (Secrets, "Types of Secret")
ckad.config-secrets-security.configmap-envfrom-invalid-keys | wrong (needs research) | The card matches the cached page, but relaxed environment variable name validation (KEP-4369) is understood to be on by default in recent releases. If so, `1badkey` is no longer skipped on a v1.35 cluster. Check the live page and release notes before changing it. | "keys that are considered invalid will be skipped … Keys [1badkey, 2alsobad] from the EnvFrom configMap default/myconfig were skipped" (Configure a Pod to Use a ConfigMap, "Restrictions")
ckad.config-secrets-security.configmap-update-scenario | ambiguous | Containers A, B and C share letters with options A to D, so "Only B" and "A and B" read two ways. The choicesExplained "A (option B)" shows the clash. Patched to name the containers `app`, `web` and `job`. | "ConfigMaps consumed as environment variables are not updated automatically … A container using a ConfigMap as a subPath volume mount will not receive ConfigMap updates." (ConfigMaps, "Mounted ConfigMaps are updated automatically")
ckad.config-secrets-security.projected-token | ambiguous | The front asks "How long do the tokens last?", which invites a duration. The back gives none: it says the lifetime is bounded and ends when the Pod is deleted. | "The tokens obtained using this method have bounded lifetimes, and are automatically invalidated when the Pod they are mounted into is deleted." (Configure Service Accounts for Pods)
ckad.api-access-resources.memory-over-limit-scenario | unclear | "Pushes it to 400Mi, and stays there" describes a state the limit prevents: the container cannot sit above its limit. | "If the Container continues to consume memory beyond its limit, the Container is terminated." (Assign Memory Resources, "Exceed a Container's memory limit")
ckad.services-ingress-netpol.service-dns-cross-namespace | unclear | The front ("can resolve … by that short name only from which namespace? What does a Pod elsewhere use?") has to be read twice. | "If this fails, perhaps your Pod and Service are in different Namespaces, try a namespace-qualified name" (Debug Services, "Does the Service work by DNS name?")
ckad.services-ingress-netpol.service-env-vars-ordering | unclear | The explanation restates the back and never says what the ordering problem is: Pods created before the Service get no variables for it. | "Note there's no mention of your Service. This is because you created the replicas before the Service." (Connecting Applications with Services, "Environment Variables")
ckad.config-secrets-security.allow-privilege-escalation | unclear | "sets the kernel's no_new_privs flag on the container process, inverted" is jargon a newcomer cannot unpack. | "inverted: if allowPrivilegeEscalation is true, then no_new_privs will be set to false." (Configure a Security Context)
ckad.services-ingress-netpol.nodeport-range-scenario | minor | The evidence and explanation describe the band policy, not the rule the answer rests on: a hand-picked nodePort must be inside the configured range. | "You also have to use a valid port number, one that's inside the range configured for NodePort use." (Service, "Type NodePort")
ckad.config-secrets-security.what-is-base64 | minor (not patched) | The back's "anyone can decode it … it is not encryption" is not on the cited Distribute Credentials page. It is true, and the Managing Secrets and Secrets pages support it. Logged only. | "kubectl get secret db-user-pass -o jsonpath='{.data.password}' \| base64 --decode" (Managing Secrets using kubectl, "Decoding the Secret")
