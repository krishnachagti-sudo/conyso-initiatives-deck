prom.observability-concepts.metrics-logs-traces-contrast | fixed | Back now says logs are "not necessarily tied to a request" and metrics are "numeric data aggregated over time", and it cites the whole primer page. Primer: "they aren't necessarily associated with any particular user request or transaction"; "Metrics are aggregations over a period of time of numeric data"; a trace "records the path taken by a single request"; "A trace is made of one or more spans."
prom.observability-concepts.span-kind-client | fixed | The explanation no longer says "waiting". Traces, "Client": "'synchronous' does not refer to async/await, but to the fact that it is not queued for later processing."
prom.observability-concepts.span-kind-consumer | fixed | The "linked rather than nested" claim is gone, and the explanation now says the parent is always a producer. Traces, "Span Kind": "the parent of a consumer span is always a producer"; "Consumer": "may start long after the producer span has already ended."
prom.prometheus-fundamentals.metric-name-utf8-vs-recommended | fixed | The back now gives the documented regex, which matches its wording. Data model: "Metric names SHOULD match the regex [a-zA-Z_:][a-zA-Z0-9_:]* for the best experience and compatibility ... Metric names outside of that set will require quoting e.g. when used in PromQL."
prom.prometheus-fundamentals.remote-read-not-stable-api | fixed | The causal link is gone, and the scalability limit is now called "a separate limit". Storage, "Overview": "The read protocol is not yet considered as stable API."; "The write protocol has a stable specification for 1.0 version"; "All PromQL evaluation on the raw data still happens in Prometheus itself. This means that remote read queries have some scalability limit."
prom.promql.range-query-rejects-range-vector-root | fixed | The front's scenario is now the bare selector http_requests_total[5m], so the front and back agree. Querying basics: "Range queries only support scalar-typed and instant-vector-typed expressions." The explanation's point that rate() gives an instant vector is also sound.
prom.promql-aggregation.count-values-application | fixed | The back now says there is one series per version, and its value is the count. Operators, "count_values": "the label value is the unique sample value. The value of each time series is the number of times that sample value was present"; the example is `count_values("version", build_version)`.
prom.promql-aggregation.divide-histogram-scalar-lhs | fixed | The explanation now says only * and / (histogram on the left) work, and that + and - remove the element. Operators: "For * , all bucket populations ... multiplied by the scalar"; "For / , the histogram sample has to be on the left hand side"; "similarly for all other arithmetic binary operators in any combination of a scalar and a histogram sample, there is no result and the corresponding element is removed."
prom.promql-aggregation.scalar-vector-kb-conversion | fixed | The back now says the metric name is dropped. Operators: "In any arithmetic binary operation involving vectors, the metric name is dropped"; "Between an instant vector and a scalar, the operator is applied to the value of every data sample in the vector."
prom.promql-aggregation.scalar-vector-application | fixed | The explanation now says the metric name is dropped. Operators: "In any arithmetic binary operation involving vectors, the metric name is dropped"; the multiply-by-2 example matches.
prom.promql-aggregation.group-right-retains-right-name | fixed | The wrong "(more numerous) matches from the left" parenthesis is gone. Operators: "If group_right is used, then the metric name from the right side is retained, to avoid collisions"; the new explanation matches "the metric name from the left side is retained ... If on is used, then the metric name is dropped."
prom.instrumentation.exporter-failed-scrape-application | fixed | The explanation now pins the drawback on the 5xx pattern. Writing exporters, "Failed scrapes": "The latter is better where there's still some useful metrics you can get even with a failed scrape, such as the HAProxy exporter providing process stats. The former is a tad easier ... as up works in the usual way, although you can't distinguish between the exporter being down and the application being down."
prom.instrumentation.exposing-software-blackbox | fixed | The explanation now says the whole list needs no exporter and "direct" marks only client-library instrumentation. Exporters: "Some third-party software exposes metrics in the Prometheus format, so no separate exporters are needed"; "The software marked direct is also directly instrumented with a Prometheus client library"; exporters are for "cases where it is not feasible to instrument a given system with Prometheus metrics directly."
