{"id":"gha.publishing-versioning.branch-reference-risk","field":"explanation","value":"The docs' example targets `@main`, so every workflow that uses it picks up each update to that branch, whether or not the update breaks anything.","severity":"unsupported","reason":"v1-beta is not on the cited page; the explanation now uses the page's own @main example"}
{"id":"gha.reusable-workflows-templates.with-and-secrets-keywords","field":"explanation","value":"A reusable workflow is called from a job, so both keywords go on the calling job beside its `uses` line: `with` carries input values and `secrets` carries named secrets.","severity":"unclear","reason":"The old explanation was ungrammatical"}
{"id":"gha.custom-actions.same-repo-reference","field":"explanation","value":"`$/` resolves to the same repository at the running commit, so no checkout step is needed first; it is not available in GitHub Enterprise Server.","severity":"unclear","reason":"The explanation now says why the docs recommend `$/`"}
{"id":"gha.reusable-workflows-templates.env-not-seen","field":"front","value":"A caller's workflow-level `env` sets `REGION` to `eu-west`. The called workflow reads `env.REGION` and finds it empty. What is the right fix?","severity":"minor","reason":"The inline `env: REGION: eu-west` is not valid YAML"}
{"id":"gha.monitoring-troubleshooting.label-clash","field":"choicesExplained","value":"A and D: they limit parallel jobs and time, not runner choice. B: debug logging adds detail to the logs; it does not change which runner is chosen.","severity":"minor","reason":"The source does not support 'reports assignment'"}
{"id":"gha.monitoring-troubleshooting.artifact-expires-at","field":"back","value":"`expires_at`, a value returned for the artifact when you look it up through GitHub's programming interface.","severity":"minor","reason":"REST API is not code, so it should not be in backticks"}
{"id":"gha.custom-actions.what-is-a-javascript-action","field":"example","value":"Like running a script with the interpreter already installed on your laptop, rather than shipping your own sealed box of tools along with it.","severity":"minor","reason":"A Docker container is not a virtual machine"}
