{"id":"ckad.container-images.user-placement","field":"back","value":"After the root-needing `RUN`, so that `RUN` still runs as root while the app runs as `svc`.","severity":"ambiguous","reason":"Position relative to CMD does not matter; what matters is that USER comes after the RUN that needs root."}
{"id":"ckad.container-images.user-placement","field":"explanation","value":"`USER` sets the default user for the rest of the stage, so it covers later `RUN` instructions and, at run time, the `ENTRYPOINT` and `CMD` commands.","severity":"ambiguous","reason":"States the source's scope exactly, without implying run-time commands must follow USER in the file."}
{"id":"ckad.container-images.shell-entrypoint-signals","field":"explanation","value":"Shell form starts the program as a subcommand of `/bin/sh -c`, so it is not `PID 1`, and the shell does not pass signals on to it.","severity":"unclear","reason":"Restores the source's order of cause and effect."}
{"id":"ckad.rollouts-helm-kustomize.contrast-canary-blue-green","needs":"research","severity":"unsupported","reason":"Canary half needs the Kubernetes Managing Workloads canary passage as a second source; the cited CNCF blue-green entry does not cover it."}
{"id":"ckad.rollouts-helm-kustomize.canary-service-selector","field":"back","value":"Its selector uses only the labels both sets share, leaving out the `track` label that tells them apart.","severity":"unclear","reason":"Makes clear that track is a label key, as the source says."}
{"id":"ckad.rollouts-helm-kustomize.app-canary-selector-bug","field":"back","value":"The selector includes `track: stable`, which the canary Pods lack; select only `app: shop` so both sets match.","severity":"minor","reason":"Code in backticks, and says why the canary is missed."}
