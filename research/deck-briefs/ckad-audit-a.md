# ckad audit A: topics 01–04

Scope: decks/ckad/notes/01-container-images.json to 04-rollouts-helm-kustomize.json, 208 cards. Every card was checked against its `evidence` first. Claims beyond the evidence were checked in research/sources/ckad/ (Dockerfile reference, docker buildx build, docker image save, Jobs, Deployments, Helm "Using Helm", Kustomize, Sidecar Containers). No fetches were needed.
Beyond-evidence claims confirmed in the cache: `-P` publishes all exposed ports (Dockerfile ref, EXPOSE); `-t` format `[registry/]repository[:tag]` (buildx build options table); `Never` → Job controller starts a new Pod (Jobs, "Handling Pod and container failures"); pausing avoids unnecessary rollouts (Deployments, "Pausing and Resuming"); `helm upgrade` for a new chart version or configuration (Using Helm, "helm upgrade"); the Helm guide assumes familiarity with charts, repositories and releases (Using Helm, intro); rolling updates overlap old and new ReplicaSets (Deployments, "Updating a Deployment").

id | severity | problem | evidence (source words, with page)
---|---|---|---
ckad.container-images.user-placement | ambiguous | The back says to put `USER` "before `CMD` or `ENTRYPOINT`", implying position relative to `CMD` matters. The explanation says `USER` applies "only to what follows it", yet run-time commands are not "what follows". What matters is that the root-needing `RUN` comes before `USER`. | "the default user and group for the remainder of the current stage. The specified user is used for RUN instructions and at runtime runs the relevant ENTRYPOINT and CMD commands." (Dockerfile reference, "USER")
ckad.container-images.shell-entrypoint-signals | unclear | The explanation reverses cause and effect: "which does not pass signals, so the program is not PID 1". The source says it is not PID 1 because it runs as a subcommand of the shell, and the shell does not pass signals on. | "starts your ENTRYPOINT as a subcommand of /bin/sh -c, which does not pass signals. This means that the executable will not be the container's PID 1, and will not receive Unix signals." (Dockerfile reference, "ENTRYPOINT")
ckad.rollouts-helm-kustomize.contrast-canary-blue-green | unsupported | The canary half of the back ("both versions take live traffic at once, split by replica counts") is not in the cited CNCF blue-green entry, which mentions canary only in its navigation. It needs the Kubernetes "Managing Workloads" canary passage as a second source. | "tweak the number of replicas of the stable and canary releases to determine the ratio of each release that will receive live production traffic" (Managing Workloads, "Canary deployments")
ckad.rollouts-helm-kustomize.canary-service-selector | unclear | "leaving out track" reads like a verb phrase: a newcomer does not know `track` is a label key. | "selecting the common subset of their labels (i.e. omitting the `track` label)" (Managing Workloads, "Canary deployments")
ckad.rollouts-helm-kustomize.app-canary-selector-bug | minor | The label pairs in the back are not in backticks, and it never says the canary lacks `track: stable`. That makes it harder to read than it needs to be. | "omitting the `track` label" (Managing Workloads, "Canary deployments")
ckad.rollouts-helm-kustomize.what-is-helm (and all Helm cards) | minor | The cached Helm "Using Helm" page is flagged by Helm itself as not yet updated for Helm 4. The cards are already marked volatile. No patch: recheck when the page is updated. | "This page has not yet been updated for Helm 4. Some of the content might be inaccurate or not applicable to Helm 4." (Using Helm, top of page)
