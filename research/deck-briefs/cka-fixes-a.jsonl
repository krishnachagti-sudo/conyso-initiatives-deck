{"id":"cka.kubectl-techniques.create-configmap","field":"front","value":"What can `kubectl create configmap` build a ConfigMap from?","severity":"ambiguous","reason":"The old front also fits `kubectl create secret generic`, which builds a Secret from the same three inputs; asking about the named subcommand leaves one answer."}
{"id":"cka.kubectl-techniques.create-configmap","field":"back","value":"A local file, a directory or a literal value.","severity":"ambiguous","reason":"Back follows the reworded front."}
{"id":"cka.kubectl-techniques.create-configmap","field":"explanation","value":"The `kubectl create` reference lists exactly these three inputs for the `configmap` subcommand, so you can build one without writing a manifest.","severity":"ambiguous","reason":"Explanation follows the reworded front."}
{"id":"cka.kubectl-techniques.scale","field":"source","value":"Kubernetes documentation, “kubectl Quick Reference”, “Scaling resources”","severity":"unsupported","reason":"The command on the back is in the Quick Reference, not on the imperative-commands page the card cited."}
{"id":"cka.kubectl-techniques.scale","field":"sourceURL","value":"https://kubernetes.io/docs/reference/kubectl/quick-reference/#scaling-resources","severity":"unsupported","reason":"Points at the section that holds the command."}
{"id":"cka.kubectl-techniques.scale","field":"evidence","value":"kubectl scale --replicas = 3 rs/foo # Scale a replicaset named 'foo' to 3","severity":"unsupported","reason":"Evidence now contains the command the back gives."}
{"id":"cka.kubectl-techniques.scale","field":"explanation","value":"`scale` sets the replica count of the controller you name, written as type/name, so `rs/foo` is the ReplicaSet foo.","severity":"unsupported","reason":"Explains the command from the newly cited Quick Reference line."}
{"id":"cka.kubectl-techniques.patch","field":"back","value":"Changes specific fields of a live object, given as a patch string.","severity":"minor","reason":"The example patch string is not on the cited imperative-commands page; the back now keeps to what that page says."}
{"id":"cka.kubeadm-install.kubelet-not-newer","field":"back","value":"No. The kubelet may be older than the API server, but never newer.","severity":"ambiguous","reason":"“One minor version older” reads as the limit; the install page only says one minor version of skew is supported, with a 1.7/1.8 example, and does not state the current maximum."}
{"id":"cka.kubeadm-install.kubelet-not-newer","field":"explanation","value":"The install page’s example: a 1.7.0 kubelet should be fully compatible with a 1.8.0 API server, but a newer kubelet with an older API server is not.","severity":"ambiguous","reason":"Keeps the source’s example and direction of the rule without implying a one-version limit."}
{"id":"cka.kubeadm-install.print-join-command","field":"explanation","value":"A joining node needs a live token and the CA key hash; by default kubeadm prints that hash at the end of `kubeadm init` or through this command.","severity":"unsupported","reason":"The old “otherwise shown only at the end of kubeadm init” overstates: the join reference says the hash can also be calculated using standard tools."}
{"id":"cka.kubeadm-install.scenario-apt-mark-hold","field":"back","value":"`apt-mark hold kubelet kubeadm kubectl`, which pins the three packages at their installed versions.","severity":"unclear","reason":"“Having run …” does not read as an answer to “What stops it?”."}
{"id":"cka.lifecycle-ha-etcd.restore-etcdctl-deprecated","needs":"research","severity":"ambiguous","reason":"Card is dated for v1.35 but repeats the docs’ “slated for removal from etcd v3.6”; check which etcd version kubeadm v1.35 deploys and whether etcdctl snapshot restore still exists there, then reword the explanation."}
