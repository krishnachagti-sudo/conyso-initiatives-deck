# istio-ica audit, part a (topics 1–4: 01-architecture, 02-installation, 03-routing, 04-ingress-egress)

273 cards checked. 10 findings: 1 ambiguous, 3 unclear, 6 minor. None wrong or unsupported. 12 patch lines in `istio-ica-fixes-a.jsonl`, and none needs research. `apply-fixes --dry` passes.

Source note: the cached copy of `istio-io-egress-tls-origination-7d5c7d.txt` is missing most of the page: the TLS origination config, the security considerations and the mutual TLS setup. A garbled glossary tooltip also makes it read "configure Istio to perform TLS termination". I checked the five origination cards (`istio.tls-origination`, `istio.mtls-origination`) against the live page, fetched on 2026-09-25 with a generic User-Agent. The cache should be refreshed. Until it is, the patched `mtls-origination-sidecar` keeps its old `evidence`, a heading that the cache does hold.

id | severity | problem | evidence (source words, with page)
---|---|---|---
istio.ingress-egress.mtls-origination-sidecar | ambiguous | The front asks "where", and "At the sidecar" implies only sidecars can originate TLS. The page's own definition says a sidecar or an egress gateway can. The card also rests on a section heading alone. Rewritten to ask what makes the sidecar originate mutual TLS. | egress-tls-origination (live): "TLS origination occurs when an Istio proxy (sidecar or egress gateway) is configured to accept unencrypted internal HTTP connections, encrypt the requests…"; DestinationRule "workloadSelector: matchLabels: app: curl … tls: mode: MUTUAL credentialName: client-credential # … works only when DR has a workloadSelector"
istio.ingress-egress.encrypt-without-code | minor | The explanation implies the traffic is fully encrypted, but the task warns that the pod-to-sidecar hop is still plain text. | egress-tls-origination (live), "Additional security considerations": "Because the traffic between the application pod and the sidecar proxy on the local host is still unencrypted, an attacker … would still be able to see the unencrypted communication"
istio.ingress-egress.gateway-needs-dns | unclear | The explanation says the task "explains this by the original destination address" but never says what that address is. | egress-gateway: "the gateway receives a request with the original destination IP address which is equal to the service IP of the gateway (since the request is directed by sidecar proxies to the gateway)"
istio.installation.injection-creation-time | unclear | The scenario card has no choices, yet its back begins "B:". | sidecar-injection: "Injection occurs at pod creation time."
istio.architecture.ambient-outbound | unclear | The front's "a Service whose destination is in the mesh and has a waypoint" has to be read twice. | data-plane, Outbound: "If the destination has a waypoint proxy, in addition to being upgraded to HBONE, the request will be forwarded to that waypoint"
istio.architecture.l7-overhead | minor | The back says "an Layer 4 mesh", which is ungrammatical. | dataplane-modes: "if your requirements can be met at L4, service mesh can be delivered at substantially lower cost"
istio.installation.helm-crd-delete | minor | The explanation says the guide forbids removing the CRDs "when uninstalling". That warning sits in the migration section. The uninstall section itself only says that deleting CRDs permanently removes resources. | helm install, "(Optional) Deleting CRDs installed by Istio": "Deleting CRDs permanently removes any Istio resources you have created in your cluster."; Uninstall: "By design, deleting a chart via Helm doesn't delete the installed Custom Resource Definitions"
istio.routing.tcp-vs-tls-routes | minor | The back calls it "plain TCP", which goes beyond the evidence. The reference says opaque TCP on non-HTTP/TLS ports. | virtual-service: "An ordered list of route rules for opaque TCP traffic. TCP routes will be applied to any port that is not a HTTP or TLS port."
istio.routing.which-policy-applies | minor | The front writes `loadBalancer: RANDOM`, which is not a valid shape; the algorithm goes under `simple`. | destination-rule, LoadBalancerSettings: `simple` (SimpleLB) field
istio.installation.gateways-list | minor | The explanation's "ingress-side gateway" is not the page's term. | customize-installation: "The default profile installs one ingress gateway, called istio-ingressgateway."
