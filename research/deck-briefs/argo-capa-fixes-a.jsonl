{"id": "argo.templates-dags-artifacts.result-daemoned", "field": "explanation", "value": "It completes the docs' table of six task results; for a task that uses withItems, depends can also test .AnySucceeded and .AllFailed.", "severity": "unsupported", "reason": "Six is the size of the table, not every result depends can test; the docs add .AnySucceeded and .AllFailed for withItems tasks."}
{"id": "argo.workflows-fundamentals.hook-named-exit", "field": "front", "value": "What happens if you give a lifecycle hook the name `exit`?", "severity": "ambiguous", "reason": "Marks exit as the hook's name, so the front cannot be read as exiting a hook."}
{"id": "argo.workflows-fundamentals.node-anti-affinity", "field": "explanation", "value": "It sits inside retryStrategy, so it shapes where retried steps run; only an empty nodeAntiAffinity is allowed at present, using the kubernetes.io/hostname label as the selector.", "severity": "unclear", "reason": "Says why the field matters for retries, so the back is not read as a ban on steps sharing a host."}
{"id": "argo.workflows-fundamentals.what-is-a-lifecycle-hook", "field": "example", "value": "A hook on a deploy step that sends an alert only if that step fails, whereas an exit handler would run at the end whatever happened.", "severity": "unclear", "reason": "Concrete example showing the condition that sets a lifecycle hook apart from an exit handler."}
{"id": "argo.templates-dags-artifacts.gc-strategies", "field": "explanation", "value": "The strategy can be set for the whole Workflow or per artifact, so temporary files can go when the run completes while a final output stays until the Workflow is deleted.", "severity": "minor", "reason": "Both strategies delete; the final output is kept only until the Workflow is deleted."}
{"id": "argo.templates-dags-artifacts.what-is-a-dag", "field": "example", "value": "Like a recipe where the sponge and the icing are made at the same time, and decorating the cake waits for both.", "severity": "minor", "reason": "Replaces the hard-to-picture cooled tray with two clear parallel tasks and one dependant."}
{"id": "argo.cd-fundamentals.what-is-the-api-server", "field": "back", "value": "A gRPC/REST server that exposes the API used by the web interface, the command-line tool and CI/CD systems.", "severity": "minor", "reason": "REST is not code, so it needs no backticks."}
