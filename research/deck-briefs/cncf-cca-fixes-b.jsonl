{"id": "cca.identity-enforcement.security-relevant-prefix", "field": "front", "value": "By default, Cilium treats {{c1::all labels}} as identity-relevant, except a built-in list of exclusions such as `pod-template-hash`.", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.security-relevant-prefix", "field": "explanation", "value": "Labels such as hashes would give each pod its own identity, so they are excluded; the `labels` option can narrow the set further.", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.security-relevant-prefix", "field": "source", "value": "Cilium docs (1.20): Limiting Identity-Relevant Labels, “Limiting Identity-Relevant Labels”", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.security-relevant-prefix", "field": "sourceURL", "value": "https://docs.cilium.io/en/stable/operations/performance/scalability/identity-relevant-labels/#limiting-identity-relevant-labels", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.security-relevant-prefix", "field": "evidence", "value": "By default, Cilium considers all labels to be relevant for identities, with the following exceptions:", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "front", "value": "Under Cilium’s default label settings, which of these pod labels is left out when deriving an identity?", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20); the old key `id.payments` and all three distractors would count by default"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "choices", "value": "A) `team=payments`\nB) `tier=web`\nC) `pod-template-hash=7d9f8c`\nD) `app=shop`", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "back", "value": "C: `pod-template-hash=7d9f8c`", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "choicesExplained", "value": "A, B and D are ordinary labels; by default Cilium counts every label except a built-in list of exclusions, so they are included.", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "explanation", "value": "A hash label changes with each pod version and would split one app into many identities, so the default list ignores it.", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "source", "value": "Cilium docs (1.20): Limiting Identity-Relevant Labels, “Limiting Identity-Relevant Labels”", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "sourceURL", "value": "https://docs.cilium.io/en/stable/operations/performance/scalability/identity-relevant-labels/#limiting-identity-relevant-labels", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.identity-enforcement.app-relevant-labels", "field": "evidence", "value": "Many Kubernetes labels are not useful for policy enforcement or visibility. A few good examples of such labels include timestamps or hashes.", "severity": "wrong", "reason": "the terminology page’s `id.` prefix rule is outdated: “By default, Cilium considers all labels to be relevant for identities, with the following exceptions” (Limiting Identity-Relevant Labels, 1.20)"}
{"id": "cca.policy-rules.ipblock-default", "field": "front", "value": "By default, can `ipBlock` rules in a Kubernetes NetworkPolicy match pod or node IPs?", "severity": "ambiguous", "reason": "an either/or question (“pod or node IPs?”) answered “No” reads two ways"}
{"id": "cca.policy-rules.app-world", "field": "front", "value": "A `public-web` pod must accept connections from the internet but not from pods inside the cluster. Which ingress rule fits?", "severity": "unclear", "reason": "“without also opening it to named in-cluster pods” is hard to parse: “named” has no referent"}
{"id": "cca.policy-rules.app-agent-down", "field": "front", "value": "Cilium uses its default in-agent DNS proxy, and the agent pod on one node is restarting. What can pods on that node expect for DNS requests that their DNS policies target?", "severity": "ambiguous", "reason": "“By default, Cilium uses an in-agent DNS proxy for DNS policy enforcement. For high availability, consider using the Standalone DNS Proxy (alpha)” (DNS-based policy tutorial, Limitations): with the standalone proxy, C would be defensible"}
{"id": "cca.policy-rules.app-agent-down", "field": "choicesExplained", "value": "A would bypass the policy, which is not what the documentation describes. B is not a DNS path. C describes the optional Standalone DNS Proxy (alpha), not the default in-agent proxy.", "severity": "ambiguous", "reason": "C was explained as “Envoy’s optional DaemonSet”, but a separate DNS proxy also exists as an alpha option: “consider using the Standalone DNS Proxy (alpha)”"}
{"id": "cca.policy-rules.cidr-not-internal", "field": "explanation", "value": "Such traffic is matched by identity, through labels, services or entities, so hard-coded addresses are not needed.", "severity": "minor", "reason": "“cannot map a peer to a label-derived identity” misfits node IPs, which the source also excludes: “use an IP belonging to a node in the cluster (including host networking pods)”"}
{"id": "cca.policy-rules.kcnp", "field": "example", "value": "Like an industry-standard form that a firm now accepts alongside its own in-house version.", "severity": "minor", "reason": "“replaces” implies KCNP replaces CCNP; the source only says “Support … is available starting with Cilium 1.20” and discusses migrating"}
{"id": "cca.ingress-gateway-mesh.ben-no-annotation-port", "field": "back", "value": "Gateway API avoids implementation-specific annotations, and supports some of those features natively instead.", "severity": "unsupported", "reason": "source says “some”, not “many”: “the Gateway API provides native support for some of these features”"}
{"id": "cca.ingress-gateway-mesh.persona-infra", "field": "explanation", "value": "They run the infrastructure beneath the cluster, while cluster operators run the cluster itself.", "severity": "minor", "reason": "“sit above the cluster operators” is not in the source, which only defines each persona"}
{"id": "cca.ingress-gateway-mesh.persona-operator", "field": "explanation", "value": "Infrastructure providers run what lies beneath the cluster; application developers configure their own apps on it.", "severity": "minor", "reason": "“sit between” is a ranking the source does not give; it only defines each persona"}
