{"id":"gha.governance.new-org-token-default","field":"explanation","value":"The token can read these scopes but not write to them, so a job that needs to write must be given more access, for example with the `permissions` key.","severity":"unsupported","reason":"The source says only read access for contents and packages; it does not say other scopes get no access."}
{"id":"gha.governance.private-repo-access-setting","field":"back","value":"Whether workflows in the same owner’s other private repositories can use its actions and reusable workflows.","severity":"ambiguous","reason":"Sharing is only with other private repositories of the same owner."}
{"id":"gha.governance.retention-rest","field":"explanation","value":"OAuth app tokens and personal access tokens (classic) need the `admin:org` scope or the “Actions policies” fine-grained permission, because the call changes an organisation policy.","severity":"minor","reason":"The source states the requirement for these two token types only."}
{"id":"gha.security-hardening.self-hosted-public","field":"explanation","value":"Private and internal repositories carry a similar risk: anyone who can fork them and open a pull request, usually anyone with read access, could compromise the runner.","severity":"unclear","reason":"The old explanation switched to private repositories without saying why."}
{"id":"gha.security-hardening.what-is-an-installation-access-token","field":"front","value":"The `GITHUB_TOKEN` is an installation access token. What is an installation access token?","severity":"unclear","reason":"The old front had to be read twice."}
{"id":"gha.security-hardening.intermediate-scenario","field":"choicesExplained","value":"D: the value is still pasted into the script, so a quote in the title can still break out. B: the question asks for the docs’ preferred fix for an inline script, which this is not. C: pinning protects against changed actions, not injected titles.","severity":"minor","reason":"The reason given for B was vague."}
