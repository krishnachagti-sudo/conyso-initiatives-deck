# KCNA audit, part B (topics 09–16)

Scope: `decks/kcna/notes/09-networking.json` to `16-ecosystem-community.json`, 458 cards
(09: 86, 10: 80, 11: 61, 12: 35, 13: 28, 14: 33, 15: 72, 16: 63), read in `order`.
Checked against the saved sources in the scratch folder (`k8sdocs/`, `k8sgloss/`, `cncfgloss/`,
`prom-*.txt`, `otel-*.txt`, `opengitops-*.md`, `toc-process-README.md`, `kcomm-governance.md`,
`kenh-README.md`, `KCNA_Curriculum.txt`, `cncf-kcna.txt`, landscape README). Nothing was fetched.
For pass 2 the reader is taken to know every `introduces` term from topics 1–8 plus earlier cards
in this range. An automatic check found that every `uses` term had been taught before the card that
uses it, so the pass 2 lines below are about words that appear on a card but are not declared in `uses`.

Wikipedia check: no card in 09–16 cites Wikipedia. Nothing to flag.

Format: `id | pass | severity | problem | source text or plainer wording`

## Pass 1: correct and sourced

k8s.application-delivery.scenario-script | 1 | ambiguous | More than one answer holds. The front asks "Which GitOps principle does this break?", but choicesExplained admits that B (Pulled Automatically) and C (Continuously Reconciled) are also missing when a person runs the script. | Reword the front: "The script itself, before anyone runs it, fails which GitOps principle?" or "What is wrong with the *form* of this configuration?"
k8s.autoscaling.scenario-queue | 1 | ambiguous | Choice A is dismissed with "A scales on observed metrics such as CPU use". The HPA can also scale on external metrics such as queue length (the HPA page covers object and external metrics), so A is arguable. | Reword the front to ask "Which CNCF project is built to scale on the number of waiting events?", or change A's explanation to "the HPA needs an external-metrics adapter for this; KEDA provides event scaling out of the box".
k8s.security.what-is-a-security-context | 1 | unsupported | The example "runAsNonRoot: true stops a container from running as root" is not in the cited glossary entry, which only says the context sets "the user that processes run as, the group..., and privilege settings". | Cite the "Configure a Security Context" task page, or change the example to one the glossary supports, e.g. "runAsUser: 1000 makes the container's processes run as user 1000".
k8s.security.scenario-run-as-nonroot | 1 | unsupported | "refuse to start if its image would run as root" describes kubelet behaviour. The cited PSS "Restricted" row only says "Containers must be required to run as non-root users" and lists the field. | Add the Security Context task page as a source, or reword: "You want the Pod to be required to run as non-root, as the Restricted standard demands. Which field?"
k8s.ecosystem-community.what-are-open-standards | 1 | unsupported | The only source is the CSI glossary entry. Nothing cited calls CRI, CNI and CSI "open standards" or says you can "swap a container runtime, network plugin or storage driver without changing Kubernetes". | Add the CRI and CNI glossary entries (the CRI entry: "defines the main gRPC protocol for the communication between... kubelet and container runtime"). Mark "open standards" as the curriculum's term, and present the swap statement as an inference.
k8s.troubleshooting.scenario-crash-logs | 1 | minor | The back (`kubectl logs <pod> -c <container> --previous`) is not the text of any choice. Choice A reads `kubectl logs <pod> --previous`. | Make the back "A) `kubectl logs <pod> --previous` (add `-c <container>` in a multi-container Pod)."
k8s.troubleshooting.what-is-kubectl-exec | 1 | minor | "with -i and -t it opens an interactive shell" overstates. The flags attach your terminal, and the shell comes from the command you run (`sh`). | Source: "You can run a shell that's connected to your terminal using the -i and -t arguments to kubectl exec, for example: kubectl exec -it cassandra -- sh". Plainer: "with -it and a shell command such as `sh`, you get an interactive shell."
k8s.security.what-are-pod-security-standards | 1 | minor | Example: "A Pod that *mounts* the node's host network" is the wrong verb. Pods use the host network; they do not mount it. | "A Pod that uses the node's host network (hostNetwork: true) passes Privileged but fails Baseline and Restricted."
k8s.networking.what-is-the-network-model | 1 | minor | "any Pod can reach any other Pod directly" drops the source's qualifier. (The later card pods-talk-without-nat does include it.) | Source: "It ensures that (barring intentional network segmentation): All pods can communicate with all other pods". Add "unless the network is deliberately segmented".
k8s.networking.what-is-cni | 1 | minor | "on Linux, container runtimes use CNI" says all runtimes; the source says most. The sentence also comes from the networking index page, not the cited Network Plugins page. | Source (networking index): "On Linux, most container runtimes use the Container Networking Interface (CNI)". Add that page as a second source.
k8s.networking.what-is-a-service-mesh | 1 | minor | The explanation lists "retries" among the features built into each service. The glossary lists encryption, common metrics and traffic insight, not retries. | Change "retries" to "traffic insight", following the source: "encrypt communications..., provide common metrics..., or provide detailed insight into traffic".
k8s.networking.cn-list-service-meshes | 1 | minor | The front describes the item as "infrastructure that manages traffic between services". That wording is not in the Definition, which only lists "service meshes". | "The CNCF definition lists typical cloud native technologies: containers, ___, multi-tenancy, microservices... What is the missing one?"
k8s.networking.nodeport-range | 1 | minor | The explanation calls --service-node-port-range an "API server flag", but the cited section says only "a range specified by --service-node-port-range flag". | Drop "API server", or cite the kube-apiserver reference.
k8s.networking.env-var-ordering | 1 | minor | "which is one reason DNS is almost always set up" is an inference presented as if the source said it. The source recommends DNS but gives no reason. | "DNS-based discovery has no such ordering problem."
k8s.networking.ingress-frozen | 1 | minor | "new features go into Gateway API" is an inference. The note says only that Ingress gets no further changes and that Gateway is recommended. | "Being GA, Ingress keeps its stability guarantees, but it will get no further changes."
k8s.networking.gateway-roles | 1 | minor | "a cluster operator runs Gateways, and an application developer writes routes" is not stated in the cited Design principles. They describe only what each role is "concerned with". | Source: Cluster Operator "is typically concerned with policies, network access, application permissions"; Application Developer "with application-level configuration and Service composition".
k8s.security.no-user-object | 1 | minor | "or an identity provider" is not on the cited page, which names client certificates, passwords, plain tokens, bootstrap tokens and JWTs. | "User identities come from outside, for example from client certificates or tokens."
k8s.security.contrast-mutating-validating | 1 | minor | "so validators check the final version of the object" is an inference not stated on the cited page. | Source: "In the first phase, mutating admission controllers are run. In the second phase, validating admission controllers are run."
k8s.application-delivery.gitops-source-of-truth | 1 | minor | The explanation quotes the OpenGitOps glossary ("canonical state store, from which GitOps takes its name"), but the only source cited is the Cloud Native Glossary. | Add "OpenGitOps, GitOps Glossary, State Store": "Git, from which GitOps derives its name, is the canonical example used as this state store".
k8s.application-delivery.principle-pulled-automatically | 1 | minor | "The agents run within the managed system" comes from the glossary's "Software System" entry, not from principle 3 as cited. | Add the source "OpenGitOps Glossary, Software System: 'The management agents within each runtime'".
k8s.observability.otel-collector | 1 | minor | "The OpenTelemetry docs call it a proxy" is attributed to OTel docs, but the card cites only the Kubernetes Observability page. | Add "OpenTelemetry documentation, What is OpenTelemetry?": "The OpenTelemetry Collector, a proxy that receives, processes, and exports telemetry data".
k8s.observability.metrics-api-aggregation | 1 | minor | "That is why it must be installed separately and registered as an API extension" is an inference; the source does not say "registered as an API extension". | Source: "Your cluster must run Metrics Server or another implementation that provides the API."
k8s.ecosystem-community.what-is-kep | 1 | minor | The explanation (kubernetes/enhancements repo, owned by SIG Architecture, several releases) comes from the Enhancements README, not from the cited governance section. | Add "Kubernetes Enhancements repository, README" as a source.
k8s.ecosystem-community.what-is-cla | 1 | minor | "All Kubernetes contributors must sign the CNCF CLA" is from governance.md, not from the cited glossary. | Add "Kubernetes Community Governance Model, CLA": "All contributors must sign the CNCF CLA".
k8s.ecosystem-community.what-is-cncf | 1 | minor | The explanation ("neutral home for collaboration") and the example (Prometheus, Fluentd, gRPC) come from the KCNA curriculum, not from the cited glossary. | Add "KCNA Exam Curriculum, page 3" as a source.
k8s.ecosystem-community.what-is-landscape | 1 | minor | "It shows how Kubernetes fits into the wider ecosystem" is not in the README. | Source: "intended as a map through the previously uncharted terrain of cloud native technologies".
k8s.ecosystem-community.scenario-production-proven | 1 | minor | Only kubernetes.io is cited, but the card also asserts that KEDA is Graduated. | Add "Kubernetes documentation, Autoscaling Workloads, Event driven Autoscaling": "KEDA is a CNCF-graduated project".
k8s.ecosystem-community.contrast-sig-wg | 1 | minor | "Both are open community groups" is stated for SIGs, but no source states it for WGs. | Source (governance): "SIGs are voluntary groups which operate in the open and anyone can join, Committees do not have open membership". Plainer: "SIGs are open to anyone; committees are not."
k8s.ecosystem-community.scenario-fluentd | 1 | minor | Choice B is dismissed only because "Envoy is not among the logging tools the Kubernetes docs list". That does not say what Envoy is, so the learner cannot rule it out on meaning. | No saved source defines Envoy. Either cite Envoy's docs ("Envoy is an edge and service proxy"), or drop Envoy as a choice.
k8s.ecosystem-community.what-are-maturity-levels | 1 | minor | The figure licence reads "B · CC BY-SA 4.0", but the drawing restates a tier-C source (CNCF TOC process, no licence). The drawing may be ours, but the tier letter suggests the source is open. | Keep CC BY-SA for our drawing, and note "facts from a tier-C source, in our own words" in the credit.
(licence, 51 cards) | 1 | minor | 32 Prometheus cards, 13 "Kubernetes community" cards, 3 Enhancements cards and 3 Landscape README cards carry "B · Apache 2.0". The brief (§2, S16/S19/S20) says these are "B? … treat as C until confirmed". | Relabel them "C · Apache 2.0 pending policy owner; facts in our own words" until the policy owner confirms Apache counts as B.

## Pass 2: easy to understand

k8s.networking.what-is-a-headless-service | 2 | unclear | "It suits workloads that need to address individual Pods, and other service discovery mechanisms." is garbled: it reads as if workloads and mechanisms were the same kind of thing. `.spec.clusterIP` is also unexplained. | "Use it when clients must reach particular Pods, or when you want your own service discovery instead of the built-in one."
k8s.networking.what-is-gateway-api | 2 | unclear | The back stacks jargon ("extensible, role-oriented, protocol-aware API kinds for modelling service networking"). The example uses HTTPRoute before it is taught (9490). | "A newer, add-on set of Kubernetes APIs for routing traffic into Services. Different teams manage different parts, and it understands protocols such as HTTP."
k8s.networking.what-is-a-networkpolicy | 2 | unclear | The example uses "ingress" to mean incoming traffic, just after the Ingress object was taught (9130). A newcomer will read it as the object. The same risk applies to networkpolicy-default-open, scenario-db-lockdown and scenario-egress-blocks. | On first use: "incoming (ingress) and outgoing (egress) traffic; not the Ingress object". Example: "...allows incoming traffic only from Pods labelled role=api."
k8s.autoscaling.hpa-tolerance | 2 | unclear | "within the tolerance of 1.0, which is 0.1" reads as if the tolerance were 1.0. | "When current ÷ target is close enough to 1.0: by default, between 0.9 and 1.1 (a 10% tolerance)."
k8s.security.default-sa-permissions | 2 | unclear | The back is jargon: "default API discovery permissions granted to all authenticated principals". | "None, apart from the basic right every signed-in identity has to see which APIs exist."
k8s.security.what-are-pod-security-standards | 2 | unclear | "Three cumulative policies" does not say what cumulative means here. | "Three policies, each stricter than the last: every level keeps the rules of the one before and adds more."
k8s.troubleshooting.terminating-cause | 2 | unclear | "stops the control plane from removing it": "it" could be the Pod or the finalizer. | "The Pod has a finalizer, and an admission webhook stops the control plane from removing that finalizer, so the Pod can't be deleted."
k8s.observability.type-histogram | 2 | unclear | "buckets" and "quantiles" are not explained. | "Counts observations into ranges you choose (buckets), e.g. under 100 ms, under 500 ms. Quantiles such as 'the 95th-percentile request time' can be worked out from it."
k8s.observability.type-summary | 2 | unclear | The notation `<basename>{quantile="<φ>"}` and the symbol φ are unexplained. | Drop the notation, or say "exposed as one series per quantile, e.g. quantile="0.95", plus _sum and _count".
k8s.networking.classify-discovery | 2 | ambiguous | "Clients call 'payments' by name, and whichever Pods currently back it answer" could be answered "a Service", "DNS" or "load balancing". | "What is the general name for finding the current instances behind a service name?"
k8s.networking.what-is-clusterip | 2 | minor | The explanation uses Ingress and Gateway before they are taught (9130, 9150). | "To reach it from outside you need another type, or an entry point such as Ingress or Gateway (covered later)."
k8s.networking.what-is-nodeport | 2 | minor | "ready endpoint" has not been taught (EndpointSlice comes at 9120). | "...forwards traffic to one of the Service's ready Pods."
k8s.networking.what-is-externalname | 2 | minor | In the example, a Service "returns a CNAME". It is the DNS lookup that returns it. | "Looking up my-service in namespace prod returns a CNAME for my.database.example.com."
k8s.networking.what-is-ingress | 2 | minor | "TLS termination" and "name-based virtual hosting" are unexplained. | "...decrypt HTTPS for the Services (TLS termination) and serve several website names from one IP (name-based virtual hosting)."
k8s.networking.what-is-dual-stack | 2 | minor | "address family" is unexplained. | "It lets workloads use IPv4 or IPv6, including for traffic leaving the cluster."
k8s.networking.what-is-istio | 2 | minor | "aggregates telemetry" uses a term not taught until topic 15. | "...and collects traffic data (telemetry)."
k8s.networking.headless-dns-records | 2 | minor | "A or AAAA records" is unexplained. | "IPv4 (A) or IPv6 (AAAA) address records pointing directly at each Pod."
k8s.networking.service-dns-name-form | 2 | minor | The placeholder domain "cluster-domain.example" may confuse. | Add "usually cluster.local, e.g. my-svc.my-namespace.svc.cluster.local".
k8s.networking.kube-proxy-linux-modes | 2 | minor | iptables, ipvs, nftables and kernelspace are bare names. | Add "(different Linux and Windows kernel features for forwarding packets)".
k8s.networking.ingress-fanout-vs-name | 2 | minor | "Host header" is unexplained. | "...by host name (the website name the client asked for)".
k8s.networking.gateway-kind | 2 | minor | "listeners" is unexplained. | "...with listeners (the ports and protocols it accepts traffic on)".
k8s.networking.classify-dual-stack | 2 | minor | A newcomer may not recognise "fd00::" as an IPv6 address. | "...both a 10.x IPv4 address and an fd00:: IPv6 address".
k8s.security.what-is-a-rolebinding | 2 | minor | ServiceAccounts appear before they are taught (10120). The same applies to binding-subjects. | Move what-is-a-serviceaccount before 10100, or gloss it: "ServiceAccounts (identities for workloads, covered next)".
k8s.security.what-is-a-security-context | 2 | minor | SELinux, AppArmor and seccomp are unexplained. | "...Linux security modules such as SELinux, AppArmor or seccomp".
k8s.security.admission-not-reads | 2 | minor | "custom verbs such as connect" is opaque. | "(and some special actions, such as connecting to a Pod through the API server)".
k8s.security.admission-webhooks | 2 | minor | "webhook" is never taught. | "...run your own logic as webhooks (HTTP calls from the API server to a service you run)".
k8s.security.psa-audit | 2 | minor | "audit log" and "audit annotation" are not taught. | "Allows the Pod, but records the violation in the API server's audit log (its record of requests)."
k8s.security.multitenancy-implementations | 2 | minor | "virtual control plane" is unexplained. | "...or a separate virtual control plane (its own API server and controllers) for each tenant, sharing the worker nodes".
k8s.security.scenario-tenant-crds | 2 | minor | StorageClasses and webhooks appear before StorageClass is taught (11070). | Keep "such as CRDs", and drop the others or gloss them.
k8s.storage.what-is-stateful-vs-stateless | 2 | minor | The example "a search that is lost when you close the window is stateless" is confusing: the search is lost, not the app. | "A search engine that forgets your search once you close the window is stateless."
k8s.storage.what-is-a-pv | 2 | minor | NFS and iSCSI are unexplained. | "(network file shares, network disks or a cloud disk)".
k8s.storage.what-is-dynamic-provisioning | 2 | minor | "provisioner (volume plugin)" appears before volume plugins are taught (11110). What-is-a-storageclass has the same problem. | Move what-is-a-volume-plugin before 11070, or gloss it as "(the code that creates the storage)".
k8s.autoscaling.hpa-not-daemonset | 2 | minor | "scale subresource" is unexplained. | "...through a standard 'scale' handle that lets the replica count be changed; Deployments and StatefulSets have one."
k8s.autoscaling.scenario-hpa-tolerance | 2 | minor | "100m of a metric" is read as 100 milli-units without being told so. | "targets a value of 100 and the current average is 105".
k8s.application-delivery.what-is-continuous-delivery | 2 | minor | "acceptance environment" is unexplained on the back. | "...deployed into a test or staging (acceptance) environment".
k8s.application-delivery.what-are-gitops-principles | 2 | minor | The back never names the four principles, so the answer is buried. | "Four requirements: Declarative, Versioned and Immutable, Pulled Automatically, Continuously Reconciled."
k8s.application-delivery.ci-start-end | 2 | minor | "artifact" is unexplained. | "...ends with a tested build (an artifact, such as a container image) ready for continuous delivery".
k8s.troubleshooting.pending-causes | 2 | minor | "hostPort" is unexplained. | "...or a hostPort (a fixed port on the node itself), which limits where it can be placed".
k8s.troubleshooting.exec-not-enough | 2 | minor | "distroless images" is unexplained. | "...as with distroless images, which contain only the app, with no shell or tools".
k8s.troubleshooting.node-shell-host | 2 | minor | "IPC, network and PID namespaces" is unexplained jargon. | "The debugging Pod shares the node's network and can see its processes, but isn't privileged unless you ask for it."
k8s.troubleshooting.scenario-no-shell | 2 | minor | "process namespace" is unexplained. | "...and --target lets it see the app container's processes".
k8s.observability.what-is-prometheus | 2 | minor | "time series" is used before it is taught (15090). | "...stores metrics as time series (values recorded with a timestamp)".
k8s.observability.what-are-metric-types | 2 | minor | The back is vague and never names the four types. | "Counter, gauge, histogram and summary: the four kinds of metric Prometheus client libraries offer."
k8s.observability.what-is-opentelemetry | 2 | minor | "telemetry" and "instrumented" are not taught. | "...for producing and sending telemetry (traces, metrics and logs). 'Instrumenting' code means adding the calls that emit it."
k8s.observability.what-is-otlp | 2 | minor | "spans", "collector" and "backend" are all new at once. | "Spans (timed steps of a request) go to a collector (a relay) or straight to a backend (the store you search)."
k8s.observability.system-logs-location | 2 | minor | journald and systemd are unexplained. | "To journald (the system log service) on machines that use systemd..."
k8s.observability.trace-export | 2 | minor | Uses "OpenTelemetry Collector" before it is taught (15230). | Move otel-collector before 15210, or gloss it as "(a relay that processes telemetry)".
k8s.observability.classify-streaming-sidecar | 2 | minor | The front diagram labels the container "streaming container", which gives the answer away. Classify-sidecar-agent has the same problem ("logging-agent" inside my-pod). | Accept this, or crop or overlay the labels on the front.
k8s.ecosystem-community.what-is-committee | 2 | minor | "Steering Committee" is never explained. | "...decided by the Steering Committee (the project's top governing body)".
k8s.ecosystem-community.kcna-key-projects | 2 | minor | The explanation covers Prometheus and Fluentd but never says what Envoy is. | Add a sourced line on Envoy, e.g. "Envoy is a service proxy" (from Envoy's docs).

## Counts

Cards checked: 458. Problem lines: 81 (51 cards share one licence line).
- wrong: 0
- unsupported: 3
- ambiguous: 3 (2 pass 1, 1 pass 2)
- unclear: 9
- minor: 66 (26 pass 1, including the grouped licence line; 40 pass 2)
