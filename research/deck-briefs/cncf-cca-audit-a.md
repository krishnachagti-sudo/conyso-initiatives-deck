# cncf-cca audit, part a (topics 01-components, 02-ipam-routing, 03-ebpf, 04-identity-enforcement; 275 cards)

All 81 scenario cards checked: every choicesExplained names exactly the three wrong letters, and every back's letter matches its choice. Commands and snippets checked against the cached pages: `kubeadm init --skip-phases=addon/kube-proxy`, `kubectl -n kube-system exec ds/cilium -- cilium-dbg status | grep KubeProxyReplacement`, `kubectl get ciliumendpoints --all-namespaces -o json`, `loadBalancer.algorithm=maglev`, `loadBalancer.mode=dsr`, `bpf.masquerade=true`, `ipam.cilium.io/ip-pool` and `require-pool-match`, the tunnel ports 8472/UDP and 6081/UDP, `ipv4-native-routing-cidr`, `auto-direct-node-routes` and `enable-ipv4-masquerade`. All match. The native-routing figure matches the Routing page. Two patches cite a new tier B page (Limiting Identity-Relevant Labels, docs.cilium.io 1.20.2, checked live). The dry run flags only "source not cached" for those two, so run `node build/cache-sources.mjs --only=cncf-cca` after applying.

cca.identity-enforcement.identity-kvstore | wrong | The card says identities are coordinated through the key-value store. The Cilium 1.20 default is CRD allocation (CiliumIdentity), and the card contradicts cca.components.crd-default. Its source, the terminology page, describes the older kvstore mode | "CRD identity allocation uses Kubernetes custom resource CiliumIdentity to represent a security identity. This is the default behavior of Cilium and works out of the box in any K8s environment without any external dependency." (Cilium Operator, #crd-identity-garbage-collection)
cca.identity-enforcement.security-relevant-prefix | wrong | The `id.` prefix rule, taken from the stale terminology page, is not the 1.20 default: every label counts except a built-in list of exceptions | "By default, Cilium considers all labels to be relevant for identities, with the following exceptions:" (Limiting Identity-Relevant Labels, #limiting-identity-relevant-labels)
cca.identity-enforcement.app-relevant-labels | wrong | The keyed answer rests on the same `id.` rule. By default, `team=payments` and `tier=web` do count toward an identity | same passage; "!pod-template-hash Ignore all pod-template-hash labels" (same page)
cca.identity-enforcement.app-packet-identity | ambiguous | Wrong option C ("looks the source address up in a list pushed to every node") describes the IP-to-identity cache (`cilium-dbg bpf ipcache list`), which a well-read candidate could defend. Replaced with a plainly wrong option | "Validate that the IP cache is synchronized correctly by running cilium-dbg bpf ipcache list" (troubleshooting); "not in the critical path for any forwarding or network policy decision" (component overview)
cca.ebpf.vs-verifier-privs | ambiguous | The back splits privileges and verification cleanly, but ebpf.io's verification step also checks the loader's privileges | "It validates that the program meets several conditions, for example: The process loading the eBPF program holds the required capabilities (privileges)." (ebpf.io, #loader--verification-architecture)
cca.ipam-routing.dsr-enable | unsupported | The card leaves out that DSR needs native routing, so a newcomer on the default encapsulation mode would enable it and fail. The front's "what else" also reads oddly | "It requires Cilium to be deployed in Native-Routing, i.e. it will not work in Encapsulation mode." (kubeproxy-free, #dsr-mode)
cca.ipam-routing.sc-native-cidr | unsupported | "Cilium refuses to start the datapath" is an inference; the source says only that both options must be set | "The following configuration options must be set to run the datapath in native routing mode" (routing, #id5)
cca.components.envoy-ds | unsupported | The back says Ingress and Gateway API support expose Envoy through a LoadBalancer or NodePort Service; the source says "often" | "Cilium's ingress support (both for Ingress and Gateway API) often uses a Loadbalancer or NodePort Service to expose the Envoy Daemonset." (gateway-api)
cca.components.arch-figure | unclear | media/cca-components.svg puts the Hubble CLI in the "Once per cluster" band beside the Operator and Relay. The CLI is a client tool that connects to Relay or a local server, not a deployed component. Fix the SVG (move the CLI outside the cluster, pointing at Relay); there is no field patch | "The Hubble CLI (hubble) is a command-line tool able to connect to either the gRPC API of hubble-relay or the local server" (component overview, #hubble)
cca.components.agent | minor | The example says kube-proxy writes iptables rules, but KCNA teaches three kube-proxy modes | KCNA k8s.networking.kube-proxy-linux-modes
cca.identity-enforcement.app-default-deny-off | minor | A DNS-intercepting policy is a layer 7 rule. The docs' example also sets `matchPattern: "*"`, because enableDefaultDeny does not cover layer 7, as the deck's own default-deny-l7 card says | "Adding a layer-7 rule that does not include a layer-7 allow-all will cause drops, even when default-deny is explicitly disabled." (policy intro, #endpoint-default-policy)
cca.ebpf.what-benefit | minor | Re-taught: KCNA k8s.cloud-native.what-is-ebpf already says eBPF runs in the kernel without changing it or loading kernel modules | ebpf.io #what-is-ebpf
cca.ebpf.sc-benefit | minor | Re-taught: the same point as KCNA what-is-ebpf, in scenario form | ebpf.io #ebpfs-impact-on-the-linux-kernel
cca.ipam-routing.podcidr | minor | Partly re-taught: CKA cka.kubeadm-install.what-is-pod-network-cidr already covers a Pod range split into one range per node | kubernetes host-scope page
cca.identity-enforcement.app-mode-default | minor | Partly re-taught: KCNA networkpolicy-default-open and CKA default-non-isolated-ingress already say an unselected Pod allows all traffic; only the Cilium mode name is new | policy intro, #policy-enforcement-modes
cca.identity-enforcement.app-direction | minor | Partly re-taught: the same shape as CKA app-independent-isolation (a policy for one direction leaves the other open) | policy intro, #endpoint-default-policy
