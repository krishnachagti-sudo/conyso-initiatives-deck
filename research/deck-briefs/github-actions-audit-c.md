# github-actions audit, part C (topics 9–12)

Cards checked: 379 (09-governance, 10-runners, 11-secrets-variables, 12-security-hardening). Every scenario card's choicesExplained letters were checked against its choices and answer by script: all match. Every back of a scenario card matches one of its choices. Volatile runner labels checked against the cached reference table: `ubuntu-latest` and `ubuntu-24.04` link to Ubuntu2404-Readme, `windows-latest` and `windows-2025` to Windows2025-Readme, and `ubuntu-26.04` exists as a separate label. YAML and command snippets (`permissions`, `concurrency`, `runs-on` groups and labels, `env` intermediates, `gh secret`, `gh attestation verify`, `config.sh`) match the docs' syntax.

id | severity | problem | evidence (source words, with page)
---|---|---|---
gha.governance.new-org-token-default | unsupported | Explanation "Only these two scopes get even read access by default" goes beyond the source, which says the token has read (not write) access for `contents` and `packages`; it does not say that every other scope has no access at all. | Disabling or limiting GitHub Actions for your organization, "Configuring the default GITHUB_TOKEN permissions": "By default, when you create a new organization, `GITHUB_TOKEN` only has read access for the `contents` and `packages` scopes."
gha.governance.private-repo-access-setting | ambiguous | The back says "other repositories of the same owner"; the setting admits only private repositories, and a newcomer would read the back as including public ones. | Managing GitHub Actions settings for a repository: "Actions and reusable workflows in your private repositories can be shared with other private repositories owned by the same user or organization."
gha.governance.retention-rest | minor | Explanation says "Tokens need…"; the source states the requirement only for OAuth app tokens and personal access tokens (classic). | REST API endpoints for GitHub Actions permissions, "Set artifact and log retention settings for an organization": "OAuth app tokens and personal access tokens (classic) need the admin:org scope or the "Actions policies" fine-grained permission to use this endpoint."
gha.security-hardening.self-hosted-public | unclear | Explanation jumps to private repositories without saying so, and reads as if it contradicts the back. | Secure use reference, "Hardening for self-hosted runners": "Similarly, be cautious when using self-hosted runners on private or internal repositories, as anyone who can fork the repository and open a pull request (generally those with read access to the repository) are able to compromise the self-hosted runner environment"
gha.security-hardening.what-is-an-installation-access-token | unclear | Front "What is an installation access token, as the `GITHUB_TOKEN` is one?" has to be read twice. | GITHUB_TOKEN, "About the GITHUB_TOKEN": "The `GITHUB_TOKEN` secret is a GitHub App installation access token."
gha.security-hardening.intermediate-scenario | minor | The reason for B ("changes the design rather than applying the documented fix") is vague; say that the question asks for the docs' preferred fix for an inline script. | Secure use reference, "Use an intermediate environment variable": "For inline scripts, the preferred approach to handling untrusted input is to set the value of the expression to an intermediate environment variable."
