# <Deck name>: notes for every writer (read after DIGEST.md)

Copy this file to `<slug>-writers.md` and fill in every line. Nothing here may
be left to the writer's guess; wave 2 lost 0.76M tokens to guessed formats.

- **Slug:** `<slug>`. **Family prefix** for card and concept ids: `<family>`.
  Example card id: `<family>.<topic-slug>.<card-slug>`; example concept id:
  `<family>.<concept-slug>`.
- **Builds on:** `<prerequisite slug>`, or none. Its registry terms (topic 0)
  count as taught: use them, never re-introduce them. Reuse its concept ids
  only where they already exist in `concepts/<its family>.json`.
- **Sources and their licence labels** (must match src/licences.json):
  - `<host or document>` → `"<tier> · <licence>"`
  - …
- **Saved text:** `research/sources/<slug>/` (manifest.json lists each
  document), then the scratch folder `<path>`.
- **Volatile facts:** which facts change, and the validAsOf text to use, e.g.
  `"2026-09-25 · <docs version>"`.
- **Pool:** none, or: pool cards are generated by `node build/pool-cards.mjs
  <slug>`; fill their explanation, choicesExplained, uses and conceptIDs.
- **Figures:** the figures the concept list names, where each comes from, and
  whether to crop or draw.
- **Scenarios:** anything the source's own exercises cover, so writers know
  not to copy them.
- **Deck notice** (for the main session): the naming text from the brief.
