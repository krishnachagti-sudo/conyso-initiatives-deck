# cks audit

415 cards, topics 01 to 10. Tier C cards (nine Docker best-practices cards in topic 08, the MITRE ATT&CK card in topic 10) were checked against the live pages: all of their facts hold (Container Administration Command sits under Execution; Docker's multi-stage, base image, packages, USER/sudo and tag-pinning advice matches).

id | severity | problem | evidence (source words, with page)
---|---|---|---
cks.supply-chain.trivy-license | wrong | Explanation says licences are the only one of the four image scanners Trivy skips by default; misconfiguration scanning is off by default too (the deck's own trivy-misconfig-scenario says so). The front also names the flag that is the answer. | trivy.dev container_image: "By default, vulnerability and secret scanning are enabled"; "Licenses ... It is disabled by default."; misconfig: "you should enable this feature with --scanners misconfig"
cks.rbac-api-access.stage-authz-403 | ambiguous | "Only when all the modules deny it" contradicts the Authorization page and card authz-mode-order: the first module to allow or deny decides, so one early deny gives 403. | kubernetes.io Authorization, "Authorization verdicts": "If any authorizer approves or denies a request, that decision is immediately returned and no other authorizer is consulted. If all modules have no opinion on the request, then the request is denied."
cks.host-hardening.minimal-host-os-choice | unclear | "so the threats typically start smaller" is hard to parse and is not NIST's point (attack surface). | NIST SP 800-190 p.38: "use these minimalistic OSs to reduce their attack surfaces"
cks.runtime-security.output-missing-field | unclear | Back says "a not-available marker in angle brackets" instead of the value actually printed. | falco basic-elements, Output: "<NA> is displayed instead"
cks.rbac-api-access.enable-admission-plugins | unclear | Example enables NamespaceLifecycle and LimitRanger, both already in the default set, so it does not show "beyond the default set". | admission-controllers, "Which plugins are enabled by default?": list includes "LimitRanger ... NamespaceLifecycle"; NodeRestriction is not in it
cks.ingress-node-binaries.kubelet-anonymous-off | minor | Explanation's claim that the API server uses the same flag is not on the cited kubelet page. | kubelet-authn-authz: only "start the kubelet with the --anonymous-auth=false flag"
cks.upgrading.drain-before-kubelet-minor | minor | CoreDNS remark comes from the kubeadm upgrade page, not the cited version skew page. | kubeadm-upgrade: "In the case of control plane nodes, they could be running CoreDNS Pods or other critical workloads."
cks.upgrading.scenario-worker-steps | minor | Explanation credits the worker sequence to the cited version skew page, which holds only the drain rule. | version-skew-policy: "Before performing a minor version kubelet upgrade, drain pods from that node."
cks.runtime-security.falco-r-switch | minor | "in the order given" is not stated on the cited page. | default-custom: "The switch can be specified multiple times; if is specified at least once, the rules_files key in the configuration file is ignored."
cks.runtime-security.crictl-ps | minor | Matching crictl IDs with Falco's container_id is not on the cited crictl page. | crictl page: "List all containers: crictl ps -a" (no Falco mention)
cks.host-hardening.metadata-api-risk | minor | Duplicates cks.ingress-node-binaries.metadata-contents: same fact, same source sentence. Deleted. | securing-a-cluster: "can contain cloud credentials for that node, or provisioning data such as kubelet credentials"
cks.host-hardening.metadata-api-measures | minor | Duplicates cks.ingress-node-binaries.metadata-mitigations: same three measures, same source sentence. Deleted. | securing-a-cluster: "limit permissions given to instance credentials, use network policies to restrict pod access to the metadata API, and avoid using provisioning data to deliver secrets"
