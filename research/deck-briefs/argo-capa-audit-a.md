# argo-capa audit, part a (topics 1–3: 01-workflows-fundamentals, 02-templates-dags-artifacts, 03-cd-fundamentals; 236 cards)

argo.templates-dags-artifacts.result-daemoned | unsupported | Explanation calls .Daemoned "the last of the six task results that depends can test", but depends can also test .AnySucceeded and .AllFailed on withItems tasks | "If you depend on a task that uses withItems , you can use .AnySucceeded and .AllFailed" (enhanced-depends-logic, #depends)
argo.workflows-fundamentals.hook-named-exit | ambiguous | Front "name a lifecycle hook exit" can be read as "exit a lifecycle hook"; the name needs marking as a literal | "You must not name a LifecycleHook exit or it becomes an exit handler" (lifecyclehook, #introduction)
argo.workflows-fundamentals.node-anti-affinity | unclear | Back "Running the steps on the same host" reads as "no two steps may share a host"; the explanation does not say that the field sits in retryStrategy and so governs where retries run | "nodeAntiAffinity prevents running steps on the same host. Current implementation allows only empty nodeAntiAffinity" (retrying-failed-or-errored-steps)
argo.workflows-fundamentals.what-is-a-lifecycle-hook | unclear | Example (smoke alarm "rather than at the end of the day") is abstract and does not show the contrast with an exit handler | "a LifecycleHook functions like an exit handler with a conditional expression" (lifecyclehook, #introduction)
argo.templates-dags-artifacts.gc-strategies | minor | Explanation says a final output "is kept"; the two strategies both delete, so the final output is kept only until the Workflow is deleted | "Artifacts can be deleted OnWorkflowCompletion or OnWorkflowDeletion" (artifacts, #artifact-garbage-collection)
argo.templates-dags-artifacts.what-is-a-dag | minor | Example "the cooled tray" is an odd, hard-to-picture dependency | "by specifying the dependencies of each task" (dag, #dag)
argo.cd-fundamentals.what-is-the-api-server | minor | Back puts REST in backticks as if it were code | "The API server is a gRPC/REST server which exposes the API consumed by the Web UI, CLI, and CI/CD systems." (architecture, #api-server)
