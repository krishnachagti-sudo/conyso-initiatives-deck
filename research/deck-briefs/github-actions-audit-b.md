# github-actions audit, part b (topics 5–8)

Cards checked: 246 (topics 5–8). Every scenario's choicesExplained letters match its wrong choices. There are no multi-line YAML snippets; the inline YAML was checked, and one is not valid YAML as written. The `$/` self-repository syntax (T6, T7) was confirmed against the live docs page. The starter-workflow wording in the GH-200 study guide (tier C) was confirmed: "copy scaffold, independent after creation".

gha.publishing-versioning.branch-reference-risk | unsupported | The explanation's `v1-beta` example is not on the cited page ("Using branches" in find-and-customize-actions). It appears only in "Managing custom actions", "Using branches for release management". | find-and-customize-actions: "This example targets a branch named `@main`"
gha.reusable-workflows-templates.with-and-secrets-keywords | unclear | The explanation is ungrammatical ("These are the same `with` and a `secrets` keyword placed on…"), so a reader has to read it twice. | reuse-workflows: "To pass named inputs to a called workflow, use the `with` keyword in a job. Use the `secrets` keyword to pass named secrets."
gha.custom-actions.same-repo-reference | unclear | The explanation gives only the Enterprise Server caveat and never says why `$/` is recommended. | find-and-customize-actions: "This resolves to the same repository at the running commit, so you do not need to check out the repository first."
gha.reusable-workflows-templates.env-not-seen | minor | The front writes `env: REGION: eu-west` on one line, which is not valid YAML (a nested mapping cannot sit inline like that). | reusing-workflow-configurations (reference): "Any environment variables set in an `env` context defined at the workflow level in the caller workflow are not propagated"
gha.monitoring-troubleshooting.label-clash | minor | choicesExplained B says that debug logging "reports assignment". The source does not say this. | troubleshoot-workflows, "Defining runner labels" (says nothing about debug logging and runner assignment)
gha.monitoring-troubleshooting.artifact-expires-at | minor | The back puts "REST API" in code backticks, but it is not code. | remove-workflow-artifacts: "see the `expires_at` value returned by the REST API"
gha.custom-actions.what-is-a-javascript-action | minor | The example contrasts JavaScript actions with "a whole virtual machine". The alternative, a Docker container action, is a container and not a virtual machine, so the example teaches the wrong idea. | custom-actions: "Docker containers package the environment with the GitHub Actions code."
