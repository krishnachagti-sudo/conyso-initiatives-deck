{"id":"az400.authentication-access-control.github-token-trigger-exceptions","field":"explanation","value":"These two always run because they are explicit requests. Pull requests the token opens or updates also start runs, but those wait until someone with write access approves them; other events create no run.","severity":"wrong","reason":"The source now lists approval-required pull_request runs as a further exception."}
{"id":"az400.secrets-sensitive-information.oidc-login-fails","field":"choices","value":"A) Add an Azure client secret as a repository secret\nB) Give the `GITHUB_TOKEN` write access to repository contents\nC) Add `id-token: write` to the permissions\nD) Switch to a classic personal access token","severity":"ambiguous","reason":"`write-all` includes id-token: write, so 'write access to everything' was also a fix."}
{"id":"az400.secrets-sensitive-information.oidc-login-fails","field":"choicesExplained","value":"A falls back to a stored secret, which OIDC is meant to remove. B widens what the token can do to the repository but still doesn't allow OIDC token requests. D is a GitHub credential, not an Azure one.","severity":"ambiguous","reason":"Matches the reworded choice B."}
{"id":"az400.security-compliance-scanning.committer-dedupe","field":"front","value":"Repository X, in one Azure DevOps organisation, has 25 active committers; repository Y, in another, has 15; 5 people commit to both. Both organisations use one Azure subscription. How many Advanced Security licences are used?","severity":"unclear","reason":"Introduces the two organisations that the front refers to."}
{"id":"az400.authentication-access-control.dependency-bot-approvals","field":"choices","value":"A) Give the `GITHUB_TOKEN` write access to pull requests\nB) Start the checks by hand with `workflow_dispatch`\nC) Add the workflow's bot to `CODEOWNERS`\nD) Create the pull requests with a GitHub App installation access token","severity":"unclear","reason":"Choice B no longer mixes a schedule with a manual trigger."}
{"id":"az400.authentication-access-control.dependency-bot-approvals","field":"choicesExplained","value":"A changes the token's permissions, not the approval rule for pull requests it creates. B starts separate runs by hand instead of letting the pull requests' own runs start. C affects review requests, not workflow approval.","severity":"unclear","reason":"Matches the reworded choice B."}
{"id":"az400.telemetry-kql.what-is-render","field":"back","value":"Tells the tool showing the results (the user agent, such as Log Analytics) to draw them as a chart, for example `| render timechart`.","severity":"unclear","reason":"Explains 'user agent' in everyday words."}
{"id":"az400.authentication-access-control.what-is-classic-pat","field":"back","value":"A GitHub personal access token granted broad scopes rather than fine-grained permissions, reaching every repository its owner can access.","severity":"unclear","reason":"Says what a classic token is without relying on the previous card."}
{"id":"az400.authentication-access-control.installation-access-token","field":"explanation","value":"The app is then limited by the permissions it has been given, not by any user's permissions.","severity":"minor","reason":"Removes 'only', which the source doesn't say."}
{"id":"az400.authentication-access-control.what-is-job-access-token","field":"explanation","value":"Because Azure Pipelines generates it at run time for each job, every job gets a token of its own.","severity":"minor","reason":"Drops the unsupported 'rather than stored with the pipeline'."}
