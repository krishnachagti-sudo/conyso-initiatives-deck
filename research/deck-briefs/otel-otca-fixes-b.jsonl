{"id":"otel.debugging.debug-exporter-scenario","field":"choicesExplained","value":"B routes spans to other Collectors; it shows you nothing. C: the docs name the debug exporter, not an extension, as the way to confirm data is received, processed and exported.","severity":"unsupported","reason":"Extensions do not require data access, but zPages does show live data; the old reason was false."}
{"id":"otel.sdk-pipelines.noop-scenario","field":"front","value":"A developer’s app calls only the OpenTelemetry API: no SDK is installed and no propagator is configured anywhere. Will trace context be written into outgoing request headers?","severity":"ambiguous","reason":"An SDK configured from environment variables defaults to tracecontext,baggage, so the front must rule the SDK out."}
{"id":"otel.sdk-pipelines.faas-scenario","field":"front","value":"A team runs its functions on Lambda and wants them auto-instrumented without writing setup code. Which OpenTelemetry assets fit?","severity":"ambiguous","reason":"Lambda layers fit only AWS Lambda, not any cloud function service."}
{"id":"otel.sdk-pipelines.parentbased","field":"back","value":"A sampler decorator that chooses which sampler to call from the span’s parent: none (root), remote or local, sampled or not.","severity":"unclear","reason":"Makes plain that ParentBased picks a delegate for each parent case."}
{"id":"otel.collector.attributes-not-name","field":"explanation","value":"This span processor is a Collector processor, not the SDK span processor from earlier cards. The troubleshooting docs list this mix-up as a common cause of processing that seems not to work.","severity":"unclear","reason":"Newcomers will read “span processor” as the SDK component taught in topic 4."}
{"id":"otel.collector.batch-processor","field":"front","value":"Which Collector distributions ship the Batch Processor, and at what stability level?","severity":"unclear","reason":"The back gives distributions and stability, not what the processor is."}
{"id":"otel.debugging.health-check","field":"back","value":"A Collector extension for health monitoring; you enable it by listing `health_check` under the service section’s extensions.","severity":"unclear","reason":"The back now says what the extension is for."}
{"id":"otel.collector.connector-scenario","field":"choicesExplained","value":"B: extensions add capabilities such as health monitoring; they do not join pipelines. C: a receiver cannot take data from another pipeline.","severity":"minor","reason":"Source says extensions do not require direct access to data, not that they never touch it."}
{"id":"otel.collector.two-backends-scenario","field":"choicesExplained","value":"B uses two pipelines, but the task asks for one, and one pipeline can hold both exporters. C: connectors join pipelines, not backends.","severity":"minor","reason":"States the real reason B is wrong."}
{"id":"otel.sdk-pipelines.library-deps-scenario","op":"delete","severity":"minor","reason":"Duplicate of otel.fundamentals.code-based-dependencies."}
{"id":"otel.sdk-pipelines.propagators-scenario","op":"delete","severity":"minor","reason":"Duplicate of otel.api-sdk-config.env-propagators-scenario."}
{"id":"otel.sdk-pipelines.tracer-naming","op":"delete","severity":"minor","reason":"Repeats otel.fundamentals.scope-for-a-library."}
