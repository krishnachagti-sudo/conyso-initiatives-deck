# gcp-pca audit, part a (topics 1–4)

Cards checked: 309 (01 Well-Architected Framework 92, 02 Business requirements and cost 43, 03 Reliability, backup and DR 84, 04 Compute 90). Every scenario card's choicesExplained letters were checked against its choices and key: all 63 match. The three tier C exam-guide cards (disposition strategies, success measures, disposition scenario) state facts in the deck's own words, and their scenario is original.

id | severity | problem | evidence (source words, with page)
---|---|---|---
gpca.business-requirements-cost.scn-sud | ambiguous | The front names no machine series, but SUDs apply only to listed series and the maximum is 20% or 30% depending on series. For an ineligible instance B ("None") would be right, and for N2/N2D/C2 the "up to 30%" in A is wrong. | "Sustained use discounts apply only to the machine series listed in the Eligible resources and discount percentages section." "Compute Engine offers a maximum monthly SUD percentage of either 20% or 30% depending on the resource and machine types." (compute/docs/sustained-use-discounts)
gpca.compute.scenario-slow-first-request | ambiguous | Distractor A (move to App Engine flexible) would also make the first request fast, because the flexible environment cannot scale to zero. The explanation only says it is "a bigger change than needed", so two answers can be defended. | "Scale to zero Yes No, minimum 1 instance" (appengine/docs/the-appengine-environments, comparison table)
gpca.reliability-backup-dr.failure-domain-primer | unsupported | The back defines a failure domain as "a part of the infrastructure that fails independently of others". The cited page never defines the term. It says only that a zone is to be treated as one. | "Zones should be considered a single failure domain within a region. To deploy fault-tolerant applications with high availability and help protect against unexpected failures, deploy your applications across multiple zones in a region." (docs/geography-and-regions)
gpca.well-architected-framework.rel-principle-postmortems | unsupported | The front says this principle "matches the learning focus area". The pillar lists focus areas and principles separately and does not map one to the other. | "Learning: To help prevent failures from recurring, learn from each experience, and take appropriate actions. ## Core principles … Conduct thorough postmortems" (architecture/framework/reliability)
gpca.well-architected-framework.rel-principle-observability | minor | The explanation says the principle "matches the observation focus area". This mapping is not stated. | same passage as above
gpca.business-requirements-cost.sud-primer | minor | The explanation gives "up to a 30% net discount" as though every VM can get it. The maximum is 20% or 30% by machine type, and only listed series are eligible. | "maximum monthly SUD percentage of either 20% or 30% depending on the resource and machine types" (compute/docs/sustained-use-discounts)
gpca.business-requirements-cost.scn-budget-stop | minor | The keyed answer stands, but the budgets page now offers spend cap budgets (preview) that pause usage. A newcomer would read "alerts-only budgets never cap spending" as the whole picture. | "If supported for your service, consider configuring a spend cap budget (preview) on a project." (billing/docs/how-to/budgets)
gpca.well-architected-framework.zero-trust-primer | minor | "Granted only on continuous verification" adds an "only" that the source does not use. | "access to resources is granted based on continuous verification of trust" (architecture/framework/security#core_principles)
gpca.well-architected-framework.scn-archetype-internal | minor | "Loss of a single data centre" uses the wrong unit. The archetypes are defined by zone and region outages, and a zone is not a data centre. | "Design considerations Downtime during zone outages." (architecture/deployment-archetypes#zonal)
gpca.compute.managed-versus-unmanaged | minor | "Use them only to…" overstates the source's conditional advice. | "Use unmanaged instance groups if you need to apply load balancing to groups of heterogeneous instances, or if you need to manage the instances yourself." (compute/docs/instance-groups)
gpca.reliability-backup-dr.scn-hot | unclear | "…and must need minimal intervention in a disaster" has no clear subject and has to be read twice. | "you have a DR plan that requires minimal intervention in the event of a disaster" (architecture/dr-scenarios-for-applications)
gpca.compute.what-is-a-machine-family | unclear | The explanation ("its three named examples show families aimed at quite different kinds of work") adds nothing. It should say how a family relates to series and types. | "Machine series: Machine families are further classified by series, generation, and processor type … the E series offers efficient VMs at a low cost" (compute/docs/machine-resource#vm_terminology)

Checked and found sound, with no finding: the WAF pillars and their core principles (nine reliability, four performance, eight sustainability, and five AI and ML pillar pages); the deployment archetypes and their use cases; CapEx/OpEx; billing account types; budget thresholds and scope; the order in which CUDs are applied and their exclusivity; SUD eligibility for GKE VMs; the three quota types; RTO/RPO definitions; the cold, warm and hot patterns, including "only by running HA" for hot and the historical-data example for cold; Backup and DR vaults (99-year retention, WORM); error budget formula; 99.99% = 8.64 s/day; the graceful-degradation and recovery-testing claims; the machine family descriptions (12 TB memory-optimised); the custom machine type 5% premium and eligible series; the Spot VM 91% discount, the 30 s shutdown period and the 120 s notice (Preview); MIG health checks, autoscaling signals and scale-to-zero rules; GKE Autopilot and regional clusters; Cloud Run's four resource types, billing modes and functions naming; and VMware Engine.
