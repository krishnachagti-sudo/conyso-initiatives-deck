# az-400 audit, part d: topics 14 to 18

Cards checked: 471 (topic 14: 173, 15: 82, 16: 91, 17: 53, 18: 72). Every scenario card's choicesExplained letters match its choices, leaving out the key. The anchor `#outside-collaborators` on "Roles in an organization" is correct. The heading in the source is "Outside collaborators{% ifversion repository-collaborators %} or repository collaborators{% endif %}", and that feature is ghec-only (data/features/repository-collaborators.yml). On the /en/ (fpt) page it renders as `id="outside-collaborators"`, which I confirmed on the live page. I fetched the four tier C Defender for Cloud pages (defender-for-devops-introduction, github-advanced-security-overview, quickstart-onboard-devops, quickstart-onboard-github) to check the cards that have no evidence. All of those claims are supported.

id | severity | problem | evidence (source words, with page)
--- | --- | --- | ---
az400.authentication-access-control.github-token-trigger-exceptions | wrong | The explanation says any other event triggered by the token creates no run. Pull requests the token opens or updates now do create runs, which wait for approval. This also contradicts dependency-bot-approvals. | "`pull_request` events with the `opened`, `synchronize`, or `reopened` activity types: when a workflow using `GITHUB_TOKEN` creates or updates a pull request, the resulting `pull_request` event creates workflow runs in an **approval-required** state" (github/docs data/reusables/actions/actions-do-not-trigger-workflows.md; the feature flag actions-github-token-pull-request-approval is on for fpt and ghec)
az400.secrets-sensitive-information.oidc-login-fails | ambiguous | Option B, "write access to everything" (`permissions: write-all`), also grants `id-token: write`, so B would fix the job as well as C. | "You can use the following syntax to define one of `read-all` or `write-all` access for all of the available permissions" (GitHub Docs, Workflow syntax, `permissions`); `id-token` is one of those permissions (secure-use / oidc-permissions-token reusable)
az400.security-compliance-scanning.committer-dedupe | unclear | The front says "Both organisations" but never introduces two organisations. The repositories have to be in different organisations for the subscription-level deduplication to matter. | "Users can contribute to multiple repositories or organizations and only get counted once, as long as those organizations are associated with the same Azure subscription." (github-advanced-security-billing.md)
az400.authentication-access-control.dependency-bot-approvals | unclear | Choice B, "Run the checks from a schedule with `workflow_dispatch`", mixes two different triggers (a schedule and a manual dispatch), so it has to be read twice. | n/a (wording)
az400.telemetry-kql.what-is-render | unclear | The back uses "user agent" without explaining it. A newcomer won't know that it means the tool showing the results, such as Log Analytics. | "The `render` operator instructs the user agent to visualize query results." (render-operator.md)
az400.authentication-access-control.what-is-classic-pat | unclear | "The other type" only makes sense next to the fine-grained card, and the back never says what distinguishes a classic token (scopes). | "Each token is granted specific, fine-grained permissions, which offer more control than the scopes granted to" classic tokens; "it will grant access to all repositories within the organizations that you have access to, as well as all personal repositories" (managing-your-personal-access-tokens)
az400.authentication-access-control.installation-access-token | minor | "Limited only by the permissions it has been given" adds "only", which the source doesn't say. The point is that no user's permissions apply. | "it means that the app will be limited by the permissions that have been given to the app" (about-creating-github-apps#github-apps-that-act-on-their-own-behalf)
az400.authentication-access-control.what-is-job-access-token | minor | "Rather than stored with the pipeline" is an inference the source doesn't make. | "a security token that is dynamically generated by Azure Pipelines for each job at run time" (access-tokens.md)
