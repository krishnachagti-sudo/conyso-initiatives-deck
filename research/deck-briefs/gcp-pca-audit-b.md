# gcp-pca audit, part b (topics 5–8: storage, databases, data processing and transfer, VPC networking and load balancing)

Cards checked: 86 storage, 51 databases, 34 data processing and transfer, 112 networking (283 in all) (all scenario choicesExplained letters match their choices; no card reuses a case study scenario; renamed products — Managed Service for Apache Spark, Managed Service for Apache Airflow — match the cached pages).

id | severity | problem | evidence (source words, with page)
---|---|---|---
gpca.storage.turbo-fifteen-minutes | unsupported | Explanation says that without turbo replication, replication has "no such target"; the cached locations page says nothing about default replication targets. | locations: "If you need a short and predictable recovery point objective (RPO), enable the premium turbo replication feature." (storage/docs/locations#location_recommendations)
gpca.storage.scenario-early-delete | unsupported | Choice D claims deleting after two weeks "still incurs charges for the minimum"; the cited storage-classes page names the 90-day minimum as a trade-off but says nothing about early-deletion charges. | storage-classes: "a 90-day minimum storage duration, and higher costs for data access are acceptable trade-offs for lowered at-rest storage costs" (#coldline)
gpca.storage.locked-bucket-deletion | unsupported | Explanation "Deleting the whole bucket is the only way to 'remove' a locked policy" is inference, not in the source. | bucket-lock: "Locking a bucket's retention policy is irreversible" (#policy-locks)
gpca.storage.what-is-local-ssd | unclear | "Being temporary, it offers the fastest performance" presents a cause the source does not give; the source states speed and data loss as a trade-off. | disks: "Temporary, or ephemeral, block storage offers the fastest performance among all block storage types, with the tradeoff that the stored data is lost if the VM stops for any reason." (#localssds)
gpca.storage.scenario-windows-shares | minor | Choice "Filestore Basic" names a tier the cached overview marks as legacy; plain "Filestore" tests the same point. | filestore overview: "Basic tiers (legacy)"; protocols "NFSv3", "NFSv4.1"
gpca.databases.firestore-availability | unsupported | Explanation "All Firestore databases have automatic multi-region replication" merges two sentences and contradicts the regional deployments named on the back. | firestore overview: "All Firestore databases offer high availability… Firestore has automatic multi-region data replication, strongly-consistent queries" (#key_capabilities)
gpca.databases.memorystore-standard-replicas | ambiguous | "Replicas can also serve read queries through the read endpoint" holds only when read replicas are enabled (tier table has a separate "read replicas enabled" column). | memorystore overview: "Read replicas No No Yes"; "scale the queries across five read replicas using the read endpoint"
gpca.databases.replica-third-zone | minor | Explanation "keeps read replicas working even if the primary's zone goes down" goes beyond the source, which says only that it improves reliability. | high-availability: "put a read replica in zone C to improve your reliability" (#read_replicas)
gpca.data-processing-and-transfer.what-is-managed-spark | minor | "material that says Dataproc refers to this service" overstates: the rename covers Dataproc on Compute Engine and Google Cloud Serverless for Apache Spark, not every Dataproc-branded product. | clusters-overview: "the new name for the product formerly known as "Dataproc on Compute Engine" (cluster deployment) and "Google Cloud Serverless for Apache Spark" (serverless deployment)"
gpca.vpc-networking-and-load-balancing.choose-regional-for-tls | minor | Reason for A ("terminate TLS in locations distributed worldwide") is not on the cited page, which says global load balancers are "distributed globally" and that TLS control needs a regional one. | choosing-load-balancer: "Choose a global load balancer … when you want the load balancer to be distributed globally"; "If you require geographic control over where TLS is terminated, you should use a regional load balancer." (#global-regional)
