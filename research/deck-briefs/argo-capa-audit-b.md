# argo-capa audit, part b (topics 4–7: 04-syncing, 05-cd-patterns, 06-rollouts, 07-events; 286 cards)

04-syncing integrity check (file regenerated from its writer's script): all 63 cards are whole (ids and orders unique and in sequence, every card has its front, explanation, source and evidence; the 3 cloze cards have no back, as intended). All 35 topic-4 concepts are covered, every evidence string is found word for word in the cached source, and the format (lettered "B: …" scenario backs, three choices, backticked code) matches its sibling 03-cd-fundamentals. No regeneration damage was found; the two findings below are content issues.

argo.rollouts.rollback-window-scenario | wrong | With `revisions: 2` and 6 current, the window counts the current revision, so only 5 is fast-tracked; the keyed answer 4 is outside, and none of the choices is right. The explanation ("covers the two revisions before the current one") contradicts the docs' example | "rollbackWindow : revisions : 3 … Assume a linear revision history: 1 , 2 , 3 , 4 , 5 (current) . A rollback from revision 5 back to 4 or 3 will fall within the window" (rollback, #rollback-windows)
argo.rollouts.setweight-scenario | wrong | choicesExplained says A (20 new, 20 stable) and D (25 new, 20 stable) "describe traffic-routed scaling"; with traffic routing the canary would still get 5 pods, so neither describes it | "if a Rollout has 10 Replicas and 10% for the first setWeight step, the controller will scale the new desired ReplicaSet to 1 replicas and the old stable ReplicaSet to 9" (canary, #overview)
argo.events.flow-order-scenario | wrong | choicesExplained says A and D "start with a component that only receives from the bus"; D starts with the EventBus itself | "EventSources publish the events while the Sensors subscribe to the events to execute triggers." (eventbus, #eventbus)
argo.cd-patterns.what-is-helm-valuefiles | unsupported | Back and explanation call --values "Helm's" flag; in the docs it is a flag of `argocd app set`; the example uses helm template, which the section does not show | "can be specified using the --values flag. The flag can be repeated to support multiple values files: argocd app set helm-guestbook --values values-production.yaml" (helm, #values-files)
argo.cd-patterns.tool-detection-chart-yaml | unsupported | choicesExplained says a config management plugin "is used only when configured, not detected from files"; the page does not say so, and plugins can have discovery rules | "If not, then the tool is detected implicitly as follows: Helm if there's a file matching Chart.yaml … Kustomize if … Otherwise it is assumed to be a plain directory application." (tool_detection, #tool-detection)
argo.cd-patterns.helm-ls-empty | unsupported | Explanation says "the docs link this very question, not seeing the app with helm ls"; the cited text says nothing about helm ls | "Helm is only used to inflate charts with helm template . The lifecycle of the application is handled by Argo CD instead of Helm." (helm, #helm)
argo.rollouts.canary-no-steps | unsupported | Explanation says the docs list mimicking a rolling update "as a use case"; the section says no such thing | "If the steps field is omitted, the canary strategy will mimic the rolling update behavior." (canary, #mimicking-rolling-update)
argo.events.eventbus-deprecated | unsupported | Explanation says "older material shows NATS Streaming as the default"; nothing in the cached sources says that | "There are three implementations of the EventBus: NATS (deprecated), Jetstream , and Kafka ." (eventbus, #eventbus)
argo.events.trigger-types-messaging | ambiguous | Front asks for "two" trigger types that publish a message; the list has a third, Azure Event Hubs Messages | "Kafka Messages - NATS Messages - Slack Notifications - Azure Event Hubs Messages" (trigger, #trigger-types)
argo.rollouts.what-is-workloadref | unclear | Front "What is workloadRef: a Rollout referencing a Deployment?" puts a gloss after a colon inside the question and reads as two questions | "Instead of removing Deployment you can scale it down to zero and reference it from the Rollout resource" (migrating, #reference-deployment-from-rollout)
argo.events.leader-election-opt-in | unclear | Back "By adding a specific annotation" gives nothing to learn; the source names the annotation | "annotations : events.argoproj.io/leader-election : k8s" (eventsources/ha, #kubernetes-leader-election)
argo.events.leader-election-rbac | unclear | Back "The RBAC rules the docs list for it" is empty; the source gives the rules | "apiGroups : [ "coordination.k8s.io" ] resources : [ "leases" ] verbs : [ "get" , "create" , "update" ]" (eventsources/ha, #kubernetes-leader-election)
argo.events.conditions-reset | unclear | Explanation "reset all the stakeholders of the conditions" copies opaque source wording and does not say why a reset helps | "conditionsReset : - byTime : # Reset conditions at 23:59 cron : "59 23 * * *"" (trigger-conditions, #conditions-reset)
argo.syncing.scenario-self-heal | unclear | Back "B: nothing: the app shows OutOfSync…" has two colons in a row and reads as two labels | "By default, changes that are made to the live cluster will not trigger automated sync." (auto_sync, #automatic-self-healing)
argo.syncing.scenario-synced-no-autosync | unclear | Explanation "Health is not what triggers it; only a Synced or error state stops it" has to be read twice | "An automated sync will only be performed if the application is OutOfSync. Applications in a Synced or error state will not attempt automated sync." (auto_sync, #automated-sync-semantics)
argo.cd-patterns.supported-tools-plugin | minor | Explanation lists "plain directories" and leaves out Jsonnet | "A directory of YAML, JSON, or Jsonnet manifests." (application_sources, #production)
argo.events.eventbus-kafka-leader | minor | Explanation is circular ("Kafka … changes how leaders are elected") | "By default, Argo Events will use NATS for the HA leader election except when using a Kafka Eventbus, in which case a kubernetes leader election will be used." (eventsources/ha, #kubernetes-leader-election)
