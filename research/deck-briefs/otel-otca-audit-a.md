# otel-otca audit, part a (topics 1–3: 267 cards)

Cards checked: 267 (01-fundamentals 55, 02-api-sdk-config 81, 03-signals 131). Sources: research/sources/otel-otca/ (opentelemetry.io docs and specs, tier B, CC BY 4.0; see CONTENT-POLICY.md §3).

id | severity | problem | evidence (source words, with page)
---|---|---|---
otel.api-sdk-config.contrib-plugin-scenario | ambiguous | "A plugin that exports telemetry to a popular analysis tool, maintained by the OpenTelemetry project" also describes an OTLP exporter, which the spec calls a Core package, so A/B/C plus "Core" are all live readings; the front must say the plugin is optional, separate from the SDK and not defined by the spec. | overview#contrib-packages: "some plugins, such as OTLP Exporters and TraceContext Propagators, are defined by the OpenTelemetry specification. These plugins are referred to as Core packages. Plugins and instrumentation packages which are optional and separate from the SDK are referred to as Contrib packages."
otel.fundamentals.total-otel-vs-prometheus | unsupported | Explanation and choicesExplained claim _total is "the Prometheus counter convention"; the cited naming page never mentions Prometheus, and no cached source does. | attribute-naming#do-not-use-total: "Counters SHOULD NOT append _total either because then their meaning will be confusing in delta backends." (only mention of _total)
otel.signals.source-legacy-first-party | unsupported | Explanation says formatters may switch "to a structured output"; the source says JSON, and card otel.signals.json-not-structured teaches that JSON is not automatically structured, so the deck contradicts itself. | specs/otel/logs#legacy-first-party-applications-logs: "the application’s log formatters may be reconfigured to output JSON instead of plain text"
otel.fundamentals.what-is-an-instrumented-library | unsupported | Evidence ("Client library See Instrumented library.") does not support the back's definition; the glossary's own definition does. | glossary#instrumented-library: "Denotes the Library for which the telemetry signals (Traces, Metrics, Logs) are gathered."
otel.fundamentals.zero-code-edges | unclear | Front "Zero-code solutions tell you what is happening where in your application, and can they…" is a statement fused with a question; a newcomer reads it twice. | instrumentation: "they provide information about what’s happening at the edges of your application. You can use both solutions simultaneously."
otel.api-sdk-config.what-is-anyvalue | minor | Back ties AnyValue to attributes only, but the spec defines it on its own and the deck later uses it for the log Body field; back also hides what the value types are. | common#anyvalue: "AnyValue is either: - a primitive type … - a homogeneous array of primitive type values … - a byte array. - an array of AnyValue, - a map<string, AnyValue>"
otel.signals.periodic-reader | minor | Example stacks two "like" similes ("Like a MetricReader on a timer, like a kitchen timer…"), which reads awkwardly. | sdk#periodic-exporting-metricreader: "collects metrics based on a user-configurable time interval"
