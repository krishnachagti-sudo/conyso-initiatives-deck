# az-104 audit, part b: topics 5–8

Cards checked: 377 (05 storage accounts 84, 06 files and blobs 93, 07 templates and Bicep 84, 08 virtual machines 116).
Sources: cached MicrosoftDocs Markdown in `research/sources/az-104/` (tier B, CONTENT-POLICY.md §3). No web requests were made.
Counts: wrong 0, unsupported 0, ambiguous 2, unclear 2, minor 3 (7 findings, 10 patch lines, none needing research).

id | severity | problem | evidence (source words, with page)
---|---|---|---
az104.templates-bicep.incremental-properties-scenario | ambiguous | The scenario uses a web app's settings, but the docs name web app site configuration as an exception to "reset to defaults", so B is not the only defensible key. Patched the front and choice D to use a storage account instead. | "Two common examples are **subnets on virtual networks** and **site configuration values for web apps** … If you redeploy the web app and specify an empty object for the site configuration values, the child resource isn't updated." (azure-docs, azure-resource-manager/templates/deployment-modes.md, Incremental mode)
az104.files-blobs.billing-provisioned-v1 | ambiguous | The front says "solid-state shares only, in a `FileStorage` account", but provisioned v2 also uses FileStorage and also offers SSD, so a reader has to parse "only" to rule it out. Patched the front and explanation. | "SSD and HDD \| Provisioned v2 \| FileStorage" / "SSD \| Provisioned v1 \| FileStorage" (azure-docs, storage/files/create-classic-file-share.md)
az104.storage-accounts.explorer-sas-address | unclear | The explanation "the storage account itself is not among them" suggests an account can't be attached with a SAS at all. It can, with a SAS connection string. Patched the explanation. | "Storage Explorer can connect to a storage account using a connection string with a Shared Access Signature (SAS)." (azure-docs, storage/storage-explorer/vs-azure-tools-storage-manage-with-storage-explorer.md, Shared access signature (SAS) connection string)
az104.virtual-machines.mover-owner | unclear | "assigns it a role such as Contributor, which needs Owner permissions" reads as if the Contributor role itself needs Owner. Patched the explanation. | "to assign it the required role (Contributor or User Access administrator in the source subscription), the account you use to add resources needs *Owner* permissions on the subscription." (azure-docs, resource-mover/tutorial-move-region-virtual-machines.md, Prerequisites)
az104.templates-bicep.bicep-to-arm | minor | The back says "in its standard form", which is vague; the source names it a Resource Manager JSON template. Patched the back. | "During deployment, the Bicep CLI converts a Bicep file into a Resource Manager JSON template." (azure-docs, azure-resource-manager/bicep/overview.md, About the language)
az104.files-blobs.versioning-no-account-recovery | minor | The front asks about "a storage account or container", but the back answers only for the account. Patched the back. | "Blob versioning can't help you recover from the accidental deletion of a storage account or container." (azure-docs, storage/blobs/versioning-overview.md, How blob versioning works)
az104.virtual-machines.zones-scenario | minor | American spelling "datacenter" in the front and the explanation. Patched both to "datacentre". | (spelling)

Checked and sound, beyond the evidence: every claim that goes past its `evidence` was found in the cited page. Examples: object replication copies current and previous versions; the Standard performance default; the scope limits of tenant(), subscription() and resourceGroup(); incremental mode as the recommended mode; linked and nested templates needing incremental mode; the FullResourcePayloads default; the top-menu Export template acting on selected resources; the Bicep file needing to be local; the Windows temp-disk resize combinations; the resize display "reflects the latest applied configuration"; Premium SSD guarantees; the temporary disk not being a managed disk; the resilience of availability sets compared with zones; host metrics (Network Out, Disk Write Bytes); the manual-scale portal steps; commit not affecting source networking; the source VM staying stopped after a regional-to-zonal move; Spot and regular instances in Flexible orchestration.
