{
  "name": "cert-decks",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "node build/check.mjs",
    "build": "node build/build.mjs",
    "test": "node --test"
  }
}
